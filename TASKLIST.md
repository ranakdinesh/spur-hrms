# spur-hrms Implementation Task List

Source of truth: `BUSINESS_LOGIC_REFERENCE.md`.

Status values: `Pending`, `In Progress`, `Blocked`, `Complete`.

A user-facing task is `Complete` only after backend, permissions, tests/build checks, and corresponding frontend UI are done. Frontend screens must be implemented in `/Users/dinesh/workplace/setika-new/frontend` with reference from `/Users/dinesh/workplace/setika-new/tailwind/template/src`.

| ID | Feature Slice | Status | Backend Scope | Frontend/UI Scope | Tailwind Reference |
|---|---|---|---|---|---|
| HRMS-000 | Module guardrails and task tracker | Complete | Create `AGENTS.md` and `TASKLIST.md` for module architecture and implementation tracking. | Add frontend rule coverage through AGENTS instructions. | n/a |
| HRMS-001 | Module manifest and permissions foundation | Complete | Define HRMS manifest metadata, permissions, role templates, idempotent registration contract. | Role/permission screens consume identity permissions API only. | `permission.html` |
| HRMS-002 | Full HRMS schema baseline | Complete | Add migrations for tenant setup, employee, leave, attendance, salary, celebration, notification, onboarding, and auth-support references; enable RLS. | No direct UI until feature slices are exposed. | n/a |
| HRMS-003 | sqlc query baseline | Complete | Add focused query files under `sql/queries` for each feature area and regenerate `adapters/postgres/sqlc`. | n/a | n/a |
| HRMS-004 | Shared domain constants and value validation | Complete | Add enums/constants and validators from reference into `core/domain` without leaking transport concerns. | n/a | n/a |
| HRMS-005 | Shared logging and error contract | Complete | Ensure constructors inject loggers and repositories/services/handlers log errors/warnings with context. | Surface useful frontend error states from API failures. | n/a |
| HRMS-006 | Tenant visual branding | Complete | Tenant-owned branding table, sqlc queries, repository, service, handlers, routes, public host resolution by subdomain, and branding permissions. | Tenant list action now opens Branding for selected tenant with logo/favicon upload, colors, font, layout, sidebar/topbar, and runtime branding provider. | `bussiness-settings.html`, screenshots |
| HRMS-007 | Branches | Complete | Tenant-owned branch CRUD with soft delete, RLS, super-admin tenant-scoped routes, tenant-user context routes, audit actor support, branch Manager/HR/Accounts user links, and DB tenant guard. | Super-admin tenant picker with branch management per tenant; tenant login direct branch list; create/edit/deactivate branches and assign Manager/HR/Accounts. | `companies.html`, `bussiness-settings.html` |
| HRMS-008 | Departments | Complete | Tenant-owned department CRUD with soft delete, repository, service, handlers, routes, permissions, and RLS-safe tenant scoping. | Super-admin tenant picker with department management per tenant; tenant login direct department list; create/edit/deactivate department UI. | `departments.html` |
| HRMS-009 | Designations | Complete | Tenant-owned designation CRUD with soft delete, repository, service, handlers, routes, permissions, and RLS-safe tenant scoping. | Super-admin tenant picker with designation management per tenant; tenant login direct designation list; create/edit/deactivate designation UI. | `designations.html` |
| HRMS-010 | Employment types and marital statuses | Complete | Tenant-owned employment type and marital status CRUD with soft delete, focused sqlc queries, default seed migration, lazy service seeding, repository, service, handlers, routes, permissions. | Super-admin tenant picker with lookup settings per tenant; tenant login direct lookup settings UI for employment types and marital statuses. | `preferences.html`, `bussiness-settings.html` |
| HRMS-011 | Financial years | Complete | Tenant-owned FY CRUD, active FY validation, only one active FY per tenant rule, HRMS usage flags for payroll/leave/holiday/reporting, and year-close lock metadata. | Super-admin tenant picker with FY settings per tenant; tenant login direct FY settings UI with create/edit/activate/lock/deactivate. | `bussiness-settings.html` |
| HRMS-012 | Working hours | Complete | Tenant/branch/user working-hour CRUD, fallback resolution, default tenant seeding, scope-day uniqueness, and copy-to-branch flow. | Super-admin tenant picker with working-hour management per tenant; tenant login direct UI with defaults, branch/user overrides, copy-to-branch, and resolver preview. | `attendance-admin.html`, `approval-settings.html` |
| HRMS-013 | Holidays | Complete | Holiday CRUD by FY/branch, optional holiday support, upcoming holiday queries. | Holiday calendar/list UI. | `holidays.html`, `calendar.html` |
| HRMS-013A | Multi-branch holiday creation | Complete | Reused existing single-branch holiday API contract; no schema, sqlc, permission, or handler changes. Product research outcome: modern HRMS leave/time products emphasize configurable leave/holiday rules, consolidated calendars, and payroll/attendance integration, so Setika reduces repeated holiday entry while keeping explicit branch records for attendance resolution. | Replaced the single branch select in the holiday modal with a checkbox multi-select dropdown, allowed one submit to create the same holiday for selected branches, made the `All branches` checkbox select every branch checkbox, kept a separate `Company-wide` action for global holiday behavior, and skipped duplicates client-side. Checks: `npm run build`, `npx eslint src/components/hrms/HolidaysSection.tsx`; full `npm run lint` is still blocked by unrelated existing lint errors outside this file. | `holidays.html`, `calendar.html` |
| HRMS-014 | Company policies and policy types | Complete | Policy type and policy CRUD, storage port for files, notification hook. | Policy list/upload/detail UI. | `policy.html` |
| HRMS-015 | Tenant subscriptions | Complete | Subscription model/query/service support for plan, status, employee limits. | Subscription/admin UI if exposed. | `packages.html` |
| HRMS-015A | Subscription plan catalog correction | Complete | Added super-admin managed subscription plan catalog with plan code/name, pricing, billing cycle, employee limit, trial days, active/inactive status, migration, sqlc queries, repository/service/handler/routes, permissions, and tenant subscription plan validation/default employee limit from active catalog plans. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Commercial > Subscriptions & Billing > Plans screen with create/edit/deactivate catalog UI; tenant subscription form now selects active catalog plans and displays plan names consistently. Checks: `npm run lint`, `npm run build`. | `packages.html` |
| HRMS-015B | Public and internal subscription pricing | Complete | Added subscription plan `visibility` (`public`/`internal`) and initial `price_basis` support, migration-backed seed data for public/internal plans, sqlc regeneration, domain validation, repository/service mapping, and active plan assignment support for both public and internal plans. Superseded by HRMS-015D for final capped package pricing. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added plan visibility controls to the super-admin Plans screen, internal badges in plan catalog and tenant subscription assignment/history, and clear negotiated-plan labels. Final public pricing model is now tracked in HRMS-015D. Checks: `npm run lint`, `npm run build`. | `packages.html` |
| HRMS-015C | Tenant edit catalog plan assignment | Complete | Reused existing HRMS active subscription-plan and tenant-subscription APIs from the tenant edit flow: selected catalog plans now update identity tenant `subscription_plan` and create/update the tenant's current HRMS subscription with the catalog `plan_id`, active status, preserved start/end dates, and employee limit fallback. | Tenant edit modal now loads active public/internal catalog plans, uses a real select instead of static text suggestions, labels public/internal negotiated plans, blocks activation without a selected plan, and shows an empty-catalog hint. Checks: `npm run lint`, `npm run build`. | `companies.html`, `packages.html` |
| HRMS-015D | Sensible billing plans and subscription enforcement | Complete | Added `minimum_amount`, `included_employees`, `overage_amount`, and `package_plus_overage` pricing support to subscription plans, seeded capped public/internal plans, added domain monthly billing calculation, count-active-employees query/repository support, and enforced current subscription employee cap before HRMS employee creation. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Replaced public pricing with Starter ₹999/month minimum at ₹39/user capped 50, Growth 100 ₹5,000/month including 100 plus ₹45/user capped 150, Business 250 ₹11,000/month including 250 plus ₹40/user capped 350, and Enterprise 500 ₹20,000/month including 500 plus ₹35/user capped 750. Super-admin plan catalog and tenant assignment labels now show minimums, included employees, overage, and hard caps. Checks: `npm run lint`, `npm run build`. | `packages.html`, `companies.html` |
| HRMS-015E | Super-admin tenant admin column | Complete | Added a host-level super-admin tenant list endpoint that returns identity tenant data with the tenant admin user resolved from the `TENANT_ADMIN` role, falling back to the first active tenant user for incomplete setups. Checks: `go test ./...`, `go build ./...`. | Tenant list now calls the admin tenant endpoint and shows the resolved admin name/email instead of the old `Not returned` placeholder. Checks: `npm run lint`, `npm run build`. | `companies.html` |
| HRMS-016 | Tenant provisioning bridge | Complete | Provide HRMS service hooks for identity-owned tenant creation: slug/code support, tenant defaults seeding, registration email port. | Tenant/company setup completion screens. | `companies.html`, `company-details.html` |
| HRMS-017 | Employee creation bridge | Complete | Employee creation through identity port, unique employee code, email/mobile checks via identity, role assignment, employee insert. | Create employee wizard/form. | `employees.html`, `form-wizard.html` |
| HRMS-017A | Employee create modal validation correction | Complete | n/a | Employee creation wizard now keeps create/API errors inside the modal, validates required identity/employment/document fields before moving forward, and shows inline field-level errors while values are corrected. Checks: `npm run lint`, `npm run build`. | `employees.html`, `form-wizard.html` |
| HRMS-018 | Employee list | Complete | List employees with department/branch/designation joins and inactive filtering. | Employee table/grid UI. | `employees.html`, `employees-grid.html` |
| HRMS-019 | Employee profile details | Complete | Employee detail aggregate: employee, statutory, bank, documents, lookups. | Employee detail/profile UI. | `employee-details.html` |
| HRMS-020 | Employee update | Complete | Update core employee, bank upsert, statutory upsert, validation and audit fields. | Employee edit UI. | `employee-details.html`, `form-vertical.html` |
| HRMS-021 | Employee deactivate | Complete | Soft-delete employee and call identity port to deactivate user in transaction boundary. | Deactivate action and confirmation UI. | `employees.html` |
| HRMS-022 | Employee documents | Complete | Document type CRUD, employee document metadata, storage port integration. | Document upload/list UI. | `file-manager.html`, `employee-details.html` |
| HRMS-023 | Leave types | Complete | Leave type CRUD with paid/carry-forward/consecutive-limit settings. | Leave type settings UI. | `leave-type.html` |
| HRMS-024 | Leave policies and allocation | Complete | Leave policy CRUD, monthly/fixed allocation, `DistributeInto12Months`, monthly allocation job hook. | Leave settings UI. | `leave-settings.html` |
| HRMS-024A | Tenant leave policy templates and formula setup | Complete | Used the advanced leave policy template/rule engine as the tenant-facing leave setup path. Added formula support for prorated yearly credits from joining/confirmation date, configurable rounding, decimal monthly credits, FY-aware accrual dates, and tenant-configurable comp-off/overtime metadata without changing existing legacy leave policy data. Research outcome: Indian HRMS leave setup needs configurable FY periods, leave-type entitlements, probation/confirmation eligibility, department/employment/designation scoping, pro-rata yearly credits, monthly accruals, comp-off/overtime choices, approvals, carry-forward, encashment, and payroll integration; comparable HRMS products expose customized policies by location/role/department with accrual, holiday/weekend restrictions, approvals, attendance-based earning, comp-off, reports, and payroll sync. Checks: `go test ./...`. | Added starter template buttons for the two requested models, exposed formula fields in the policy rule builder, kept custom template creation, allowed decimal numeric inputs, and displayed saved formula config on rules. Checks: `npx eslint src/components/hrms/LeaveFoundationSection.tsx`, `npm run build`. | `leave-settings.html`, `approval-settings.html`, `attendance-admin.html` |
| HRMS-024B | Scoped leave policy accrual automation | Complete | Leave accrual now resolves eligible employees from rule scope when a tenant opts into scoped auto-apply, while preserving existing manual employee-template assignments and ledger idempotency. Department/employment/designation/probation scope is enforced for both manual and auto-applied candidates. Research outcome carries forward from HRMS-024A: department/role/location-scoped policy automation is expected in modern HRMS products, but it must be explicit to avoid accidental balance credits in live tenants. Checks: `go test ./...`. | Added an auto-apply-by-scope control to the rule builder and enabled it for starter templates so tenants can create policy rules once and let accrual apply to matching employees. Checks: `npx eslint src/components/hrms/LeaveFoundationSection.tsx`, `npm run build`. | `leave-settings.html`, `approval-settings.html` |
| HRMS-025 | Leave templates, balances and ledger | Complete | Configurable leave templates/rules, employee assignments, accrual calculator foundation, balance upsert, opening/manual/monthly credits, pending/used updates, ledger audit records, balance queries. | Leave template settings, balance visibility, and ledger timeline in leave screens. | `leave-settings.html`, `leaves-employee.html`, `leave-report.html` |
| HRMS-026 | Apply leave | Complete | Date normalization, FY validation, sandwich check, overlap bitmask, pending balance update, approval row, notification hook. | Employee leave request UI. | `leaves-employee.html` |
| HRMS-026A | Employee leave self-service correction | Complete | Leave apply now defaults to the active tenant financial year when `fy_id` is omitted, and leave balance listing defaults to the logged-in actor when no employee query is supplied. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/.gocache go test ./...` in `spur-hrms` and `setika`. | Employee leave application now uses `/hrms` routes, hides employee and financial-year selectors from employees, loads current-user balances with leave type names, allows leave type selection from balance cards, simplifies full/half/custom duration selection, shows estimated days, and keeps admin selectors only in the super-admin tenant view. Checks: `npm run lint` (existing warnings only), `npm run build`. | `leaves-employee.html` |
| HRMS-026B | Leave balance onboarding and enabled leave type visibility | Complete | Employee role template now includes read-only leave type and balance permissions needed for self-service leave application. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/.gocache go test ./...` in `spur-hrms` and `setika`. | Employee leave dropdown now shows enabled tenant leave types even before balance rows exist. Tenant admins now have a visible Leave Balances entry under Time & Leave/My Team/Tenant Operations and a CSV import panel in Leave Settings for opening/current balances during migration from manual or legacy HRMS systems. Checks: `npm run lint` (existing warnings only), `npm run build`. | `leaves-employee.html`, `leave-settings.html`, `dashboard.html` |
| HRMS-027 | Approve leave | Complete | Configurable tenant approval workflows, sequential steps, all/any decision rules, reporting-manager/manager-manager/specific-user/HR-user approver resolution, transactional approval, pending-to-used balance movement, ledger writes. | Workflow setup UI and manager approval queue UI. | `approval-settings.html`, `leaves.html` |
| HRMS-028 | Reject/cancel leave | Complete | Approver rejection and applicant cancellation flows, pending balance reversal with safe DB guard, approval remarks, leave status updates, ledger writes. | Reject controls in approval queue and cancel controls with remarks in employee leave requests. | `leaves.html`, `leaves-employee.html` |
| HRMS-029 | Leave manager list/reporting | Complete | Manager-filtered leave report rows, tenant-wide report filters, and summary KPI queries. | Leave report UI with tenant picker, manager scope, filters, KPI cards, detail table, and CSV export. | `leave-report.html`, `leaves.html` |
| HRMS-030 | Attendance check-in/out | Complete | Attendance insert/update, device log support, work mode, geo fields, source tracking. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Employee attendance action UI with check-in/check-out, work mode, source, device metadata, optional punch location, and same-day timeline. Checks: `npm run lint`, `npm run build`. | `attendance-employee.html` |
| HRMS-030A | Employee attendance dashboard correction | Complete | n/a | Fixed attendance report rendering when backend report segments return `null`; employee dashboard now prompts for same-day check-in when attendance is required and shows a check-out action after check-in. Checks: `npm run lint`, `npm run build`. | `attendance-employee.html`, `dashboard.html` |
| HRMS-030B | Missing checkout regularisation workflow | Complete | Daily attendance status now resolves one-sided past punches, and same-day punches after shift end plus grace, as `incomplete` with `missing_checkout` outcome instead of counting them as clean present days. Reports and HR dashboard now count incomplete attendance separately, include these rows in exceptions, and keep attendance rate based only on completed present days. Approved missed-checkout requests continue to insert the missing checkout punch. Checks: `go test ./...` in `spur-hrms` and `setika`. | Attendance UI now shows incomplete counts, flags missing checkout days, pre-fills a missed-checkout regularisation request, and exposes incomplete counts on employee and HR dashboards. Checks: `npm run lint` (existing warnings only), `npm run build`. | `attendance-employee.html`, `attendance-report.html`, `dashboard.html` |
| HRMS-030C | Advanced attendance exception workflow expansion | Complete | Added tenant attendance exception workflows and audit events in `0053_attendance_exception_workflows.sql`, sqlc queries, domain/ports/repository/service methods, route-mode resolution by employee branch/department, auto-approval support, escalation due timestamps, request audit history, payroll-blocking flags, blocker API, and payroll period lock validation for unresolved configured exceptions. Research outcome: market HRMS patterns expect exception routing separate from base attendance policy, unresolved exception visibility before payroll close, and auditable manager/HR/auto handling for missed punches, late/early exits, remote/WFH, overtime and device/location exceptions. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Added Attendance exception workflow modal, workflow list, payroll-blocker list, and branded attendance report PDF/Excel downloads while retaining CSV. Checks: `npm run build`. | `attendance-admin.html`, `approval-settings.html`, `attendance-report.html` |
| HRMS-030D | Employee attendance checkout prompt correction | Complete | No backend changes. Research outcome: employee attendance products commonly keep check-in/check-out as self-service actions across web/mobile, with location captured at punch time; the mandatory interruption should protect missing start-of-day attendance, while checkout should remain employee-controlled when they close the day. | Employee dashboard attendance modal now appears only when same-day check-in is missing. After check-in, no checkout popup is shown immediately; the existing Check Out button remains available on the attendance card and still captures location when the employee is ready to end the day. Checks: `npm run build`. | `attendance-employee.html`, `dashboard.html` |
| HRMS-031 | Attendance status engine | Complete | Computed daily status engine with priority order: before/after employment, approved leave, explicit attendance status, check-in present, holiday, weekoff, past absent, and today/future empty. Added tenant/date summary endpoints. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Attendance summary cards and resolved daily status table added to employee attendance UI. Checks: `npm run lint`, `npm run build`. | `attendance-employee.html`, `attendance-report.html` |
| HRMS-032 | Configurable attendance policies, rosters and regularisation | Complete | Tenant attendance policy CRUD for fixed/flexi/daily/weekly/monthly roster models, WFH/location/device flags, grace/halfday/absent thresholds, roster assignment, request create/review workflow for missed punch, late/early exemption, WFH/remote, halfday/absent/overtime, and approved correction writes to attendance records. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Attendance UI now includes policy setup, roster assignment, employee exemption/regularisation request submission, approval/rejection controls, and policy-aware status outcomes. Checks: `npm run lint`, `npm run build`. | `attendance-admin.html`, `attendance-employee.html` |
| HRMS-033 | Attendance reports | Complete | Tenant/user/date range attendance report endpoint built on the HRMS-032 policy-aware status engine, including worked hours, late/early minutes, attendance/absenteeism/late rates, pending regularisation count, department rollups, daily trends, work-mode split, and employee exception lists for managers/CXO reporting. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Attendance report UI added with date range filters, KPI cards, department/daily/work-mode report blocks, late/absence/exception watchlists, detailed rows, and CSV export. Checks: `npm run lint`, `npm run build`. | `attendance-report.html` |
| HRMS-034A | Employee onboarding and document review | Complete | Added employee middle-name persistence, onboarding document metadata, encrypted-file metadata, pending/approved/rejected/resubmission review states, admin review endpoint, authenticated employee self-service profile/upload endpoints under `/employees/me`, backend file size/MIME allow-list validation, magic-byte validation for standard HR file types, required-document default preservation, approved-document upload/update lock, and computed onboarding completeness summary. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Employee creation is now a wizard with identity, personal, employment, document upload, and review steps. Employee profile supports document checklist upload, onboarding completeness cards, file restriction hints, and HR approve/reject/resubmission actions with remarks. Added `My Onboarding` employee self-service page for uploads/resubmissions after login; approved document types are hidden/locked from further employee upload. Checks: `npm run lint`, `npm run build`. | `candidates.html`, `form-wizard.html`, `employee-details.html` |
| HRMS-034 | Pay cycle configuration | Complete | Added configurable pay-cycle CRUD, attendance cutoff rules, payout timing, arrears/lock controls, India statutory toggles/default rates for PF/ESI/PT/TDS, payment export settings, and salary period resolver helpers. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Settings > Payroll Settings with tenant selection for super admin, tenant self-management, payroll rule form, India statutory defaults, payment notes, and live period preview. Checks: `npm run lint`, `npm run build`. | `payroll.html` |
| HRMS-035 | Salary templates | Complete | Added salary template CRUD, salary component item CRUD, active-template-per-FY activation flow, richer India-ready component configuration for CTC/gross/net templates, statutory/variable markers, calculation modes, bases, caps/min/max, and employer/employee contribution sides. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Settings > Salary Templates with super-admin tenant picker, FY filter, clean template grid, create/edit modals, activation action, component management panel, and India payroll preset components. Checks: `npm run lint`, `npm run build`. | `payroll.html`, `payroll-deduction.html` |
| HRMS-036 | Employee salary structure | Complete | Added employee salary assignment domain/ports/service, sqlc create/list/delete queries, transaction-scoped PostgreSQL repository and mappers, salary-template-based structure calculation, tenant and tenant-user HTTP routes, and salary template item type constraint alignment. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Employee Salary dashboard section with super-admin tenant picker, employee/FY/template selection, gross salary assignment, structure preview, assignment history, and shared API/auth usage. Checks: `npm run lint`, `npm run build`. | `employee-salary.html` |
| HRMS-037 | Salary calculation | Complete | Added employee salary calculation service over persisted HRMS-036 structures, absent deduction, special pro-rata, dynamic LWP deduction item rule, optional attendance-derived absent days, tenant and tenant-user calculation preview endpoints, and expanded salary rule tests. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added salary calculation preview controls and result cards to Employee Salary UI with month/year, optional day overrides, special pro-rata toggle, LWP item preview, and shared API/auth usage. Checks: `npm run lint`, `npm run build`. | `employee-salary.html` |
| HRMS-038 | Salary slip generation | Complete | Added tenant salary slip format schema/queries/repository, salary slip create/update/item/leave snapshot queries, generate/regenerate service with existing-slip guard, atomic item/leave snapshot replacement, tenant/user list endpoints, tenant bulk generation, PDF renderer port with local adapter, single PDF download, employee recent-month ZIP download, and HR monthly bulk ZIP download. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Payslips dashboard section with super-admin tenant picker, tenant-editable slip format, individual generation/regeneration, bulk all-staff generation, monthly list, single PDF download, HR month ZIP download, and employee recent ZIP download through shared auth/API helpers. Checks: `npm run lint`, `npm run build`. | `payslip.html`, `payslip-report.html` |
| HRMS-039 | Salary slip PDF | Complete | Replaced the interim PDF renderer with a `gofpdf` salary slip adapter, added salary slip PDF storage port/default local adapter, wired module options for custom PDF/storage adapters, uploaded generated/downloaded PDFs through the storage port, and persisted `salary_slips.pdf_path`. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Payslips UI from HRMS-038 continues to support tenant format editing, single PDF download, employee recent ZIP, and HR bulk monthly ZIP downloads. Checks: `npm run lint`, `npm run build`. | `payslip.html` |
| HRMS-040 | Employee dashboard data | Complete | Added employee dashboard aggregate domain/service/handler with profile, upcoming celebrations, attendance today/recent/month totals, leave balances/recent requests, recent payslips, policies, quick tools, onboarding status, and total work calculations. Added read-only celebration repository/mappers over existing sqlc queries. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Employee Dashboard section as tenant user landing page with profile summary, attendance KPIs, leave balances, payslip downloads, quick tools, celebrations, onboarding, and policies. Checks: `npm run lint`, `npm run build`. | `employee-dashboard.html` |
| HRMS-041 | Admin/HR dashboard data | Complete | Added tenant HR dashboard aggregate with headcount/workforce distribution, today and monthly attendance summaries, leave approval summary/recent pending requests, payroll generated/pending totals, onboarding/document review counts, policy counts, upcoming celebrations, operational alerts, and coming-soon service cards for unbuilt modules. Added tenant and super-admin tenant-scoped dashboard endpoints. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added HR Dashboard UI with super-admin tenant picker, period filter, KPI cards, attendance/leave/payroll/onboarding panels, workforce mix, alerts, celebrations, and coming-soon pages/cards for recruitment, performance, training, and engagement. Checks: `npm run lint`, `npm run build`. | `dashboard.html` |
| HRMS-042 | Celebration types | Complete | Added celebration type domain validation/defaults, sqlc create/update queries, repository CRUD, lazy and provisioning default seeding, tenant and super-admin tenant-scoped HTTP CRUD routes, and existing manifest permission usage for `celebration_types.manage`. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Settings > Celebration Types UI with super-admin tenant picker, create/edit/deactivate controls, yearly recurrence flag, and employee/user celebration flag. Checks: `npm run lint`, `npm run build`. | `bussiness-settings.html` |
| HRMS-043 | Celebrations CRUD/list | Complete | Added celebration create/update/delete sqlc queries, repository CRUD, service validation for celebration type, employee, branch, date, user/type uniqueness, enriched list items, and next occurrence sorting for yearly events. Added tenant and super-admin tenant-scoped HTTP CRUD routes. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Celebrations UI with super-admin tenant picker, type/employee/branch/date form, create/edit/deactivate actions, upcoming calendar cards sorted by next occurrence, and shared API/auth usage. Checks: `npm run lint`, `npm run build`. | `employee-dashboard.html`, `calendar.html` |
| HRMS-044 | Celebration daily job | Complete | Added reusable scheduled-job persistence with `job_locks` and `job_runs`, sqlc queries, Postgres repository/mappers, service-level lock/idempotency/run-history handling, tenant/all-tenant execution path, personal and branch-scoped group recipient resolution, active/resigned employee exclusions, email/push channel payloads behind a `CelebrationNotifier` port, and tenant/super-admin run/history APIs. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Settings > Celebration Jobs UI with super-admin tenant picker, manual date run, force retry, refreshable job history, status badges, counts, and error display. Checks: `npm run lint`, `npm run build`. | `cronjob.html` |
| HRMS-045 | Notification masters/preferences | Complete | Added notification master/preference domain models, default master seeding with logged missing-master warnings, preference CRUD/upsert, effective preference resolution, push opt-out, digest frequency, quiet hours, timezone controls, focused ports/services/repositories/mappers, tenant and super-admin APIs, and sqlc-backed query generation. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Settings > Notification Settings UI with super-admin tenant picker, tenant notification master defaults, employee/current-user effective preferences, in-app/email/push toggles, digest frequency, quiet hours, and timezone controls. Checks: `npm run lint`, `npm run build`. | `notification-settings.html` |
| HRMS-046 | Send notifications | Complete | Added preference-aware notification send service with missing-master warning/no-op, in-app inbox writes, email/push delivery logs, suppressed status for opt-outs/missing email, bulk IDs, idempotency keys, inbox filters/read operations, log filters, focused repository/mappers, and tenant/super-admin APIs. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Settings > Notification Center UI with super-admin tenant picker, bulk send form, channel selection, employee recipients, inbox preview/read-all, delivery log filters, and suppression/error visibility. Checks: `npm run lint`, `npm run build`. | `notification-settings.html` |
| HRMS-047 | Device tokens | Complete | Added active device-token uniqueness by tenant/user/device, duplicate cleanup migration, sqlc upsert/rotation queries, device-token domain validation, repository/service methods for upsert, rotated-token deactivation, user/device listing, soft delete, and tenant/super-admin APIs. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added shared device-token API helper and Notification Settings browser registration/list panel using centralized API/auth helpers, with optional provider token support for future FCM integration. Checks: `npm run lint`, `npm run build`. | n/a |
| HRMS-048 | Notification inbox | Complete | Added joined notification inbox pages with total/unread counts, unread/read filtering, notification master/category filtering for policy/celebration/general/leave notifications, search, mark read, mark unread, mark all read, archive/soft-delete, and tenant/super-admin scoped APIs. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added dedicated Inbox dashboard section with super-admin tenant picker, employee/current-user selector, search, read-state and category filters, KPI cards, pagination, refresh, mark read/unread, archive, and mark-all-read actions. Checks: `npm run lint`, `npm run build`. | `email.html`, `notification-settings.html` |
| HRMS-049 | Job positions | Complete | Added job position and location domain validation, paged/searchable position list with department/employment-type/work-mode filters, create/update/delete APIs, location CRUD, enriched list signals for location count and open requisition count, repository/service/handler wiring, and tenant/super-admin scoped routes. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Job Positions dashboard section with super-admin tenant picker, position KPIs, filters, position create/edit/deactivate form, department/employment type/work mode/headcount/budget fields, location editor, pagination, and requisition readiness signals. Checks: `npm run lint`, `npm run build`. | `job-list.html`, `job-grid.html` |
| HRMS-050 | Job requisitions | Complete | Added requisition domain validation, requisition audit log migration with RLS, create/update/list/detail APIs, paged pipeline filters, submit/approve/reject/close lifecycle actions with transition validation and log writes, position defaulting, and tenant/super-admin scoped routes. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Job Requisitions dashboard section with super-admin tenant picker, status pipeline cards, filters, create/edit form, save-and-submit flow, approve/reject/close actions, remarks audit log, and visible position signals from HRMS-049 including position code, planned headcount, budget, and open requisition count. Checks: `npm run lint`, `npm run build`. | `pipeline.html`, `job-list.html` |
| HRMS-051 | Job postings | Complete | Added sqlc-backed job posting repositories, tenant/super-admin APIs, approved requisition defaults, publish/close/expire support, and published listing expiry guard. | Added Job Postings dashboard with tenant picker, filters, status cards, requisition-based form, salary visibility, expiry action, publish, and close controls. | `job-list.html`, `job-grid.html` |
| HRMS-052 | Candidates | Complete | Added candidate domain validation, sqlc create/update/search/count queries, repository/service/handler/routes, tenant and super-admin scoped CRUD, soft delete, source/gender/search filters, and paged list support. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Candidates dashboard section with super-admin tenant picker, KPI cards, search/source/gender filters, table/grid toggle, create/edit form, resume URL metadata, salary/notice/location fields, and deactivate action. Checks: `npm run lint`, `npm run build`. | `candidates.html`, `candidates-grid.html` |
| HRMS-053 | Candidate applications | Complete | Added candidate application domain/status validation, sqlc create/update/move/search/count queries, enriched candidate/job-posting list joins, repository/service/handler/routes, tenant and super-admin scoped CRUD, resume metadata, comments, stage movement, active duplicate guard, applied/stage-change timestamps, rejection/withdrawal/source-detail fields, duplicate reference, and event audit trail. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Applications dashboard section with super-admin tenant picker, KPI cards, search/stage filters, kanban pipeline columns, stale-stage signal, application form, candidate/job posting selectors, resume/cover-letter metadata, CTC/notice/referral/source fields, source detail, duplicate marker, rejection/withdrawal reasons, comments, deactivate, move-stage controls, and stage history timeline. Checks: `npm run lint`, `npm run build`. | `candidates-kanban.html`, `pipeline.html` |
| HRMS-054 | Interview rounds | Complete | Added interview round scheduling, filters, interviewer assignment, status updates, remarks, feedback, score, decision, completion tracking, and delete support through sqlc-backed repository/service/HTTP APIs. | Added dashboard Interviews section with tenant selector, schedule grouping, filters, status actions, interview form, remarks, feedback, score, and decision capture. | `calendar.html`, `candidates.html` |
| HRMS-055 | Offer letters | Complete | Added tenant-specific offer letter templates, rendered offer content, latest-version handling, generated/sent/accepted/declined/revoked lifecycle, public signature tokens, signer identity/IP/user-agent/hash capture, and offer event audit trail through sqlc-backed repository/service/HTTP APIs. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Offers dashboard section with super-admin tenant picker, template editor, offer generation, status controls, signature link display, rendered preview, signed hash visibility, and audit trail. Checks: `npm run lint`, `npm run build`. | `candidates.html` |
| HRMS-056 | Candidate onboarding workflows | Complete | Added onboarding workflow/task CRUD, workflow default/active flags, assignment template rules for job posting, job position, department, and employment type, sqlc-backed repositories/services/handlers/routes, and assignment RLS migration. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Onboarding Flows dashboard section with super-admin tenant picker, workflow cards, task checklist builder, assignment template rules, KPI cards, and navigation entry. Checks: `npm run lint`, `npm run build`. | `form-wizard.html`, `candidates.html` |
| HRMS-057 | Candidate onboarding progress | Complete | Added start-onboarding flow with assignment/default workflow resolution, task snapshot creation, due dates, overdue indexes, task status updates, progress recalculation, completed status rule based on required tasks, event audit trail, sqlc-backed repository/service/handlers/routes, and generated sqlc code. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Candidate Onboarding dashboard section with super-admin tenant picker, start controls, workflow auto-resolution option, progress cards, overdue/stalled signals, checklist task status actions, remarks, and event trail. Checks: `npm run lint` (existing warnings only), `npm run build`. | `candidates.html`, `kanban-view.html` |
| HRMS-058 | External email provider ports | Complete | Added tenant email provider settings, SMTP/local/SendGrid delivery sender behind ports, config defaults, delivery audit fields, test-send service, sqlc-backed repository/queries/migration, and notification email delivery updates with provider status tracking. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Email Providers dashboard section with super-admin tenant picker, SMTP/SendGrid/local configuration, sender identity, SPF/DKIM/DMARC status, webhook secret, delivery hints, test-send action, and settings removal. Checks: `npm run lint` (existing warnings only), `npm run build`. | `email-settings.html` |
| HRMS-059 | SMS and WhatsApp provider ports | Complete | Added tenant communication provider settings with SMS/WhatsApp enable switches defaulting disabled, sqlc migration/queries, domain validation, repository/service/handler/routes, MSG91 SMS/WhatsApp adapter, Gupshup WhatsApp adapter, local development adapter, config defaults, preserved secret updates, and channel test-send result tracking. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added SMS & WhatsApp dashboard section with super-admin tenant picker, per-channel enable/disable switches, MSG91/Gupshup/local settings, secret redaction, webhook secret, status cards, provider hints, test-send action, and settings removal. Checks: `npm run lint` (existing warnings only), `npm run build`. | `otp-settings.html` |
| HRMS-060 | Storage provider port | Complete | Added tenant storage provider settings with S3/MinIO selection, sqlc migration/queries, domain validation, repository/service/handler/routes, MinIO Go SDK S3-compatible adapter, tenant-aware policy/document/salary-slip storage, config defaults, preserved credential updates, bucket connectivity tests, file size/content-type guards, and object key prefixes for profile photos/resumes/future flows. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Storage Providers dashboard section with super-admin tenant picker, S3/MinIO selector, bucket/endpoint/region credentials, SSL/path-style options, prefix/public URL, max file size/content-type controls, test connection, and settings removal. Checks: `npm run lint` (existing warnings only), `npm run build`. | `file-manager.html` |
| HRMS-061 | Push provider port | Complete | Added provider-neutral push port with local and Firebase FCM HTTP v1 adapters, tenant push provider settings, service-account OAuth JWT flow, sqlc migration/queries, repository/service/handler/routes, config defaults, preserved private key updates, Flutter device-token fan-out, push delivery audit updates, and provider readiness in notification preferences. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Push Providers dashboard section with super-admin tenant picker, local/FCM settings, service account fields, Android/iOS/web toggles, TTL/collapse/click/image controls, test push to Flutter FCM token, secret redaction, and settings removal. Checks: `npm run lint` (existing warnings only), `npm run build`. | `notification-settings.html` |
| HRMS-062 | Legacy PBKDF2 verifier support | Complete | Added reusable PBKDF2 verifier package with SHA-256/SHA-1, Django-style `$` and colon formats, base64/hex digest support, constant-time comparison, unit tests, service-level verification helper, and identity-owned legacy password migration port/module option so HRMS never directly mutates auth tables. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Login flow now understands migration-required, migration-message, and migrated-success states from identity responses while keeping auth token storage centralized. Checks: `npm run lint` (existing warnings only), `npm run build`. | `login.html` |
| HRMS-063 | Seed data on tenant creation | Complete | Tenant provisioning now seeds default working hours, guarantees an active financial year, employment/marital/designation defaults, celebration types, notification masters, and default pay cycle inside the existing service-level system transaction. Provisioning status now reports active FY readiness, notification master count, and pay-cycle readiness. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Existing tenant setup completion UI displays the provisioning seeded map, so new notification master and pay-cycle seed counts are visible after tenant creation. Checks: `npm run lint` (existing warnings only), `npm run build`. | `bussiness-settings.html` |
| HRMS-064 | Scheduled jobs registration | Complete | Added registered scheduled-job catalog and generic locked runner for celebration notifications, monthly leave accrual, daily holiday reminders, and monthly salary readiness reminders. Jobs use tenant/job/date locks, persisted run history, idempotent skip/force semantics, metadata for observability/backfill, existing leave-ledger idempotency, and request-level/service-level logging. Generic job list/run/history APIs were added while preserving celebration-specific routes. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Expanded Celebration Jobs into a Scheduled Jobs console with tenant selection, registered job details, idempotency/schedule metadata, manual run controls, force retry, and per-job run history. Checks: `npm run lint` (existing warnings only), `npm run build`. | `cronjob.html`, `cronjob-schedule.html` |
| HRMS-065 | Core unit tests | Complete | Expanded table-driven coverage for leave day units/date normalization/invalid types, sandwich leave with weekoffs and holidays, monthly leave distribution, salary earnings/deductions/LWP/special proration, next yearly occurrence including rollover/leap-day cases, and PBKDF2 parse/verify/error handling. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | n/a | n/a |
| HRMS-066 | Integration and handler tests | Complete | Added focused service/handler tests for attendance status helper priority and worked-minute calculation, leave approval step completion rules, handler tenant/actor extraction, and JSON error response handling. Existing salary generation regeneration guard, transaction-scoped service methods, and RLS-capable repository boundaries are covered by compile/build and focused core tests without requiring a live Postgres instance in this smoke suite. Checks: `go test ./...`. | n/a | n/a |
| HRMS-067 | Frontend HRMS shell/navigation | Complete | n/a | HRMS dashboard shell includes grouped navigation, section routing, tenant-aware sections, and module screens from setup through payroll, recruitment, notifications, integrations, jobs, and dashboards. Download flows now use shared `apiDownload`/`saveBlobDownload` helpers so auth/API handling remains centralized. Checks: `npm run lint` (existing warnings only), `npm run build`. | `layout-modern.html`, `dashboard.html` |
| HRMS-067A | Dashboard menu consolidation | Complete | n/a | Reorganized the dashboard sidebar into a single Main Menu with accordion submenus for Workspace, Super Admin, People, Recruitment, Time & Leave, Payroll, Company Setup, Communication, Culture, and Commercial. Removed always-expanded long menu lists and hid non-working placeholder platform menus from the primary navigation. Checks: `npm run lint`, `npm run build`. | `layout-modern.html`, `dashboard.html` |
| HRMS-067B | Superadmin dashboard route guard | Complete | n/a | Superadmin navigation no longer exposes employee-only Dashboard/My Onboarding/HR Dashboard entries, and stale superadmin employee-dashboard or HR Dashboard state now renders the Inbox instead of calling employee or broken HR dashboard APIs. Checks: `npm run lint`, `npm run build`. | `dashboard.html` |
| HRMS-067C | Superadmin SaaS operator dashboard | Complete | n/a | Added a SmartHR/Tailwind-style superadmin landing dashboard for HRMS SaaS operations: compact welcome/status strip, MRR/ARR projection from assigned catalog plans, active/pending/inactive tenant health, plan coverage, public/internal catalog mix, risk queue for missing plans/admins/lifecycle issues, recent tenant setup view, and shortcuts to tenants, plans, subscriptions, and roles. Superadmin now lands on this overview after login, while stale employee/HR dashboard states route to the same safe operator view. Checks: `npm run lint` (existing warnings only), `npm run build`. | `dashboard.html` |
| HRMS-067D | Superadmin menu IA correction | Complete | n/a | Replaced the superadmin sidebar IA with a practical HRMS SaaS platform console: Dashboard, Tenants, Subscriptions, Tenant Operations, Hiring, System Operations, Access Control, Support, and Settings. Removed role-name/vague labels like Super Admin and Workspace from the superadmin menu while preserving tenant-admin/HR menus separately. Checks: `npm run lint` (existing warnings only), `npm run build`. | `dashboard.html`, `layout-modern.html` |
| HRMS-067E | Tenant user home and header navigation correction | Complete | n/a | Removed the tenant-user Workspace submenu and replaced it with a direct Home sidebar item. HR Dashboard is no longer grouped under Home; it is exposed through the header only for HR-capable tenant users. Header Home, Inbox, Notifications, and HR Dashboard icons now navigate to real sections instead of being decorative. Checks: `npm run lint` (existing warnings only), `npm run build`. | `dashboard.html`, `employee-dashboard.html` |
| HRMS-067F | Employee self-service dashboard redesign | Complete | n/a | Rebuilt the employee dashboard using the Tailwind `employee-dashboard.html` pattern: real profile/photo card, branded primary/secondary/tertiary metric blocks, attendance punch/status card, leave balances by leave type, payslip list, upcoming events, policies, and quick actions. Removed unconfigured onboarding from normal employee view and changed the top user chip from generic Admin to the logged-in user's name/email/initials. Checks: `npm run lint` (existing warnings only), `npm run build`. | `employee-dashboard.html` |
| HRMS-067G | Employee dashboard profile bar correction | Complete | n/a | Removed the oversized employee profile/detail card from the dashboard. Employee identity is now a compact photo/name/status bar with only lightweight chips for employee code, branch, and employment type, leaving the primary dashboard area for attendance and leave work. Checks: `npm run lint` (existing warnings only), `npm run build`. | `employee-dashboard.html` |
| HRMS-067H | Header profile dropdown correction | Complete | n/a | Removed employee identity from the dashboard body and made the top-right user chip the dedicated identity surface. The user chip now opens a profile dropdown with name, email, role, tenant hint, Home/Inbox shortcuts, and Logout inside the dropdown. Checks: `npm run lint` (existing warnings only), `npm run build`. | `dashboard.html`, `employee-dashboard.html` |
| HRMS-067I | Tenant-admin dashboard landing correction | Complete | n/a | Tenant admins and HR users now land on the HR Dashboard instead of employee self-service, and Home shortcuts in the sidebar, header, and profile dropdown use the same role-aware landing target. This prevents tenant admins without employee profiles from seeing `failed to load employee dashboard`. Checks: `npm run lint` (existing warnings only), `npm run build`. | `dashboard.html` |
| HRMS-067J | HR dashboard nullable API list correction | Complete | n/a | HR Dashboard now normalizes nullable API list fields and numeric summary groups before rendering, so tenant admins do not hit runtime errors when leave requests, attendance departments, celebrations, alerts, or upcoming services are returned as `null`. Checks: `npm run lint` (existing warnings only), `npm run build`. | `dashboard.html` |
| HRMS-067K | Tenant-admin access control restoration | Complete | n/a | Restored Access Control for tenant admins with Users and Roles & Permissions. Tenant admins can now list tenant users, see assigned roles, add users with selected roles, edit user profile/active status, assign roles through the identity API, and edit role permission sets from the existing Roles screen. Checks: `npm run lint` (existing warnings only), `npm run build`. | `roles-permissions.html`, `dashboard.html` |
| HRMS-067L | Global email provider configuration | Complete | Added global email configuration from `setika/.env` for `EMAIL_PROVIDER=smtp|sendgrid`, shared from/from-reply settings, SMTP settings, and SendGrid settings. Wired the host app into HRMS config, made HRMS delivery use global settings only instead of tenant email-provider rows, made tenant email-provider mutation read-only when global email mode is active, and added SendGrid support for identity verification emails. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/.gocache go test ./...` in `setika` and `spur-hrms`. | Removed tenant Email Providers from dashboard navigation so admins use global env configuration instead of per-tenant setup. Checks: `npm run lint` (existing warnings only), `npm run build`. | `dashboard.html` |
| HRMS-067M | Docker local full-stack deployment | Complete | Added root `docker-compose.local.yml` for local full-stack verification using the existing host Postgres database, Redis, MinIO, backend, and frontend. Fixed backend Docker build to use the repo root so local `spur-hrms` replace works, added host storage config pass-through for MinIO, and added Docker `.env` defaults for host DB access and object storage. Checks: `docker compose --env-file setika/.env -f docker-compose.local.yml config --quiet`, `GOCACHE=/Users/dinesh/workplace/setika-new/.gocache go test ./...` in `setika`, Docker build/start, backend health `200`, frontend `200`, MinIO health OK. | Added `frontend/Dockerfile` for the active Next.js app and published Docker frontend on `3002` because local `3000` was already in use; backend is published on `8087` because local `8086` was already in use. | `docker-compose.local.yml`, `Dockerfile.backend`, `Dockerfile` |
| HRMS-067N | Employee baseline role and team menu segregation | Complete | Made `EMPLOYEE` the baseline identity role for HRMS-created users and tenant signup admins, added startup sync to enable HRMS for existing tenants, assign Employee to existing tenant users, and align Employee/Manager role permissions, changed `MANAGER` into an additive team-scope role template, and added permission tests for role boundaries. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/.gocache go test ./...` in `setika` and `spur-hrms`. | Sidebar now uses the logged-in user's real permissions, separates employee self-service under My Work from manager/HR team workflows under My Team, adds a Manager role template, and keeps Employee selected/non-removable when assigning tenant user roles. Checks: `npm run lint` (existing warnings only), `npm run build`. | `roles-permissions.html`, `dashboard.html` |
| HRMS-067O | Setika long-lived login session | Complete | Added Setika-only `/setika/auth/login` endpoint that delegates credential validation to identity and re-signs the verified access JWT with `SETIKA_LOGIN_SESSION_TTL=4320h` so Setika sessions survive for months without changing the shared identity module. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/.gocache go test ./...` in `setika`. | Login now calls the Setika-specific endpoint, Remember me is enabled by default, and centralized auth storage persists long-lived sessions in `localStorage` while still clearing both local/session storage on logout. Checks: `npm run lint` (existing warnings only), `npm run build`. | `login.html`, `dashboard.html` |
| HRMS-067P | Remove developer/task copy from product UI | Complete | n/a | Removed task IDs, backend/API/internal implementation wording, manual-style explanations, and placeholder-like copy from user-facing dashboard, attendance, employee, document, storage, notification, contact, and shared error text. Checks: UI copy scan, `npm run lint` (existing warnings only), `npm run build`, Docker rebuild/start, backend `/healthz`, frontend `/dashboard`. | `dashboard.html`, `attendance-report.html`, `employees.html`, `file-manager.html`, `notification-settings.html` |
| HRMS-067Q | Tenant-branded email templates | Complete | Added tenant-editable email subject/text/HTML template fields to notification masters, migration `0049_notification_email_templates.sql`, sqlc query regeneration, default notification templates for leave/policy/celebration/general stages, and branded HTML rendering at email delivery using tenant display name, colors, and public logo URL when available. Checks: `sqlc generate`, `go test ./...` in `spur-hrms`, `go test ./...` in `setika`. | Extended Notification Settings so tenant admins/superadmins can edit email subject, text body, optional HTML body, and see which notification masters have templates. Checks: `npm run lint` (existing warnings only), `npm run build`. | `notification-settings.html` |
| HRMS-068 | End-to-end smoke checklist | Complete | Final backend smoke completed with no blockers: `sqlc generate`, `go test ./...`, `go build ./...`. | Final frontend smoke completed with no blockers: `npm run lint` (existing warnings only) and `npm run build`. | n/a |
| HRMS-069 | Employee master issue backlog | Complete | Added LWF applicability to employee statutory data through migration/query/domain/port/repository/mappers, persisted statutory flags during employee creation, retained update support, and added reporting-manager validation against active employee users with self-manager protection on update. Last name remains optional and single address remains the saved profile/edit field. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Employee onboarding now includes reporting-manager selection plus PF/ESIC/PT/LWF applicability controls during HRMS ID creation. Employee edit/profile now shows reporting manager, preserves saved address values, and exposes LWF beside PF/ESIC/PT. Checks: `npm run lint` (existing warnings only), `npm run build`. | `employees.html`, `employee-details.html`, `form-wizard.html` |
| HRMS-070 | Probation and leave eligibility backlog | Complete | Added employee probation metadata with status/start/end/duration/confirmed-date/payroll-staff fields, six-month minimum probation validation for payroll staff, automatic payroll probation defaults, sqlc-backed migration/query/repository/mappers, and domain tests. Leave accrual now evaluates employee probation state, blocks Earned Leave/EL credits during probation unless a tenant explicitly configures probation-only EL, and leave requests reject EL while the employee is still on probation. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Employee onboarding/profile edit now captures probation and payroll-staff settings, profile summary shows probation state/end date, and employee leave requests warn that EL is blocked during probation while SL/CL remain available when configured. Checks: `npm run lint` (existing warnings only), `npm run build`. | `employees.html`, `leave-settings.html`, `leaves-employee.html`, `leave-report.html` |
| HRMS-071 | Exit management workflow backlog | Complete | Added employee exit workflow persistence with request/task/event tables, RLS, sqlc queries, repository/mappers, service transitions for submit/approve/reject/cancel/complete, notice/relieving dates, standard offboarding checklist, audit trail, and completion gate that deactivates employee/identity only after all tasks are completed or waived. Added module permissions for `employee_exits.*`. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Employee Exits dashboard section with super-admin tenant picker, HR initiation form without address fields, status/search filters, progress cards, approval/reject/cancel/complete actions, checklist status updates, F&F/access/asset/handover/interview signals, blocked-completion errors, and event trail. Checks: `npm run lint` (existing warnings only), `npm run build`. | `employees.html`, `employee-details.html`, `fnf.html` |
| HRMS-072 | Payroll import, statutory and reporting backlog | Complete | Added payroll operations schema/queries/repository/service for period lock/unlock audit, payroll imports with row-level validation/error reports, consolidated salary sheet data/export, attendance/LOP/LWP reconciliation, and state/branch-specific PT/LWF statutory rules. Salary slip generation now rejects locked periods and adds PT/LWF deduction lines when employee statutory applicability and matching rules exist. Added `payroll_*` permissions. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added Payroll Operations dashboard section with super-admin tenant picker, period controls, lock/unlock actions, consolidated salary sheet, CSV export, reconciliation exceptions, CSV payroll import/apply flow, import row details, and PT/LWF rule management. Checks: `npm run lint` (existing warnings only), `npm run build`. | `payroll.html`, `employee-salary.html`, `payslip-report.html`, `payroll-deduction.html` |
| HRMS-073 | Credential resend and login recovery backlog | Complete | Added HRMS credential recovery service behind identity-owned ports: resend recovery credentials, reset temporary passwords, trigger recovery email delivery, persist sent/failed credential events with tenant-scoped RLS audit history, expose tenant/super-admin APIs, and declare `employees.credentials.manage` in the module manifest. | Added employee profile login recovery controls for resend credentials, temporary password reset, delivery success/failure feedback, and recent credential event history. | `EmployeesSection.tsx`, `employee_credentials.sql`, `0044_employee_credential_events.sql` |
| HRMS-074 | Appointment and experience letters backlog | Complete | Added employee letter templates and generated letters for appointment, experience, and relieving letters with RLS schema, sqlc queries, repository/mappers, service rendering, PDF renderer/storage port, optional signature tokens, approval/send/sign/revoke/download lifecycle events, latest-version tracking, employee document linkage, tenant/super-admin APIs, public signature APIs, and `employee_letters.*`/template permissions. Checks: `sqlc generate`, `go test ./...`, `go build ./...`. | Added HR Letters dashboard section with super-admin tenant picker, tenant-scoped template management, employee/document-type selection, variable-backed generation, status actions, PDF download through shared helpers, signature link/hash display, document linkage state, and audit timeline. Checks: `npm run lint` (existing warnings only), `npm run build`. | `letters.html`, `employee-details.html`, `file-manager.html` |
| HRMS-074A | Payroll pay groups and phased pay runs | Complete | Added pay-group and phased-pay-run schema in `0052_pay_groups_pay_runs.sql`, sqlc queries, domain validation, ports, repository/mappers, service workflow, HTTP APIs, tenant/super-admin routes, pay-run employee readiness, audit events, freeze/generate/lock/unlock lifecycle, and `pay_groups.*` / `pay_runs.*` manifest permissions. Research outcome: Zoho-style pay groups and common payroll staging patterns expect one monthly schedule with independent group readiness, staged processing by branch/department/type/tag/manual override, and duplicate employee/month protection; Setika adopts that via existing salary-slip generation and payroll reconciliation instead of a second payroll engine. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Added Payroll Operations pay-group/pay-run console with group list, employee preview/counts, manual override modal, new pay-run modal, pay-run readiness status table, assess/freeze/generate/lock/unlock actions, and info-button helper text for setup guidance. Checks: `npm run build`, local dev server `curl -I http://localhost:3000/dashboard` returned 200; in-app browser tool was blocked by internal sandbox metadata error. | `payroll.html`, `employee-salary.html`, `payslip-report.html` |
| HRMS-074B | Intelligent reporting foundation | Complete | Added tenant-scoped reporting foundation with RLS migration `0051_reporting_foundation.sql`, sqlc queries, domain validation, ports, repository/mappers, service methods, default catalog seeding, HTTP APIs, export-job audit, saved views, schedules, snapshots, drilldown/filter metadata, and `reports.view/manage/export/schedule` manifest permissions. Research outcome: modern HRMS reporting needs a catalog-first workspace with grouped reports, role-aware scopes, saved views, exports, scheduled delivery metadata, and period-close snapshots before deeper analytics are layered in. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Added Reports workspace with tenant picker, catalog search/module/scope filters, selected-report context, saved-view modal, export modal, schedule modal, snapshot modal, history tables, favorite saved views, export queue visibility, schedule list, and info-button-only helper text to keep forms uncluttered. Checks: `npm run build`. | `dashboard.html`, `report.html`, `analytics.html` |
| HRMS-074C | Workforce and employee intelligence reports | Complete | Added reusable report dataset/export service with workforce composition, movement, and data-quality catalog codes backed by employee master data, summary metrics, tenant branding, and direct PDF/XLSX rendering. Research outcome: HR leaders expect workforce packs to cover headcount mix, movement, org allocation and missing master data, with management-ready exports rather than only on-screen dashboards. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Reports catalog now shows workforce intelligence reports and direct branded PDF/Excel downloads. Checks: `npm run build`. | `dashboard.html`, `employees.html`, `employee-details.html` |
| HRMS-074D | Attendance, leave and productivity intelligence reports | Complete | Added attendance health/exception and leave utilization/liability report datasets using the existing attendance status/report engine and leave report rows/summary, including branded PDF/XLSX exports and payroll-blocking attendance exception data. Research outcome: time-and-leave intelligence must reuse operational attendance/leave truth, show exceptions and approval exposure, and export for payroll cutoffs and manager reviews. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Attendance and Leave Reports now include direct branded PDF/Excel downloads in addition to CSV. Checks: `npm run build`. | `attendance-report.html`, `leave-report.html`, `dashboard.html` |
| HRMS-074E | Payroll, statutory and compliance intelligence reports | Complete | Added payroll readiness, payroll cost, payroll reconciliation, consolidated salary sheet, payroll compliance, and compliance readiness datasets with tenant branding, direct PDF/XLSX exports, payroll blocker visibility, salary/reconciliation reuse, and report catalog entries. Research outcome: payroll/compliance reporting must be audit-ready, period-scoped, exportable, and tied to reconciliation/blocker status before statutory filing or salary finalization. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Payroll Operations now downloads consolidated salary sheet and reconciliation as branded PDF/Excel; Reports catalog exposes payroll/compliance export packs. Checks: `npm run build`. | `payroll.html`, `payslip-report.html`, `payroll-deduction.html` |
| HRMS-074F | Recruitment, onboarding, exit and lifecycle reports | Complete | Added talent lifecycle report datasets for recruitment funnel, source effectiveness, onboarding completion, probation confirmations due, exit pipeline, and exit/F&F readiness. Datasets reuse candidate applications, interview rounds, job requisitions, candidate onboarding task progress, employee probation dates, and employee exit workflow/readiness data, with tenant-branded PDF/XLSX export through the shared report dataset renderer. Research outcome: market HRMS reporting expectations for talent lifecycle focus on funnel conversion, source quality, stage aging, time-to-fill, onboarding task/document gaps, probation actions due, resignation pipeline, clearance readiness, and exit reasons as exportable management packs rather than isolated raw lists. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Added a Talent lifecycle reports panel in the Reports workspace with hiring funnel, source effectiveness, onboarding, probation, exit pipeline, and F&F readiness packs plus direct PDF/Excel downloads. Checks: `npm run build`. | `pipeline.html`, `candidates.html`, `form-wizard.html`, `fnf.html` |
| HRMS-074G | AI insight and anomaly reporting layer | Complete | Added insight storage and audit tables in `0054_insights_foundation.sql`, sqlc queries, repository/mappers, service methods, permissions, and APIs for deterministic insight refresh, queue listing, lifecycle status updates, and audit events. Current deterministic rules cover attendance anomalies, low attendance/engagement health, leave exposure, payroll reconciliation anomalies, compliance/master-data gaps, probation action due, onboarding delay, and exit/F&F readiness risk. Added an `InsightScoringPort` so future HRMS-092/093 AI scoring can enrich the same insight contract without changing handlers/UI. Research outcome: market AI HRMS features are expected to be explainable, confidence-scored, reviewable by humans, and actionable from existing HR workflows; Setika adopts deterministic rules first with auditability and avoids opaque auto-decisions. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Added Insights workspace in the dashboard with summary risk cards, severity/status/category filters, anomaly queue, explain-why panel, recommendations, audit trail, rule refresh, and review modal for reviewing/resolving/dismissing/overriding insights. Checks: `npm run build`. | `dashboard.html`, `analytics.html`, `attendance-report.html`, `payroll.html` |
| HRMS-074H | Flutter mobile API migration foundation | Complete | Added new Setika API profile configuration for dev/staging/prod with `SETIKA_API_BASE_URL` override, removed the hard-coded OAuth client secret default, added a unified authenticated Setika Dio client with tenant host/code headers and shared error-envelope parsing, extended secure session storage for refresh token, expiry, tenant id/host, roles and permissions, and migrated mobile login/branding/dashboard/attendance punch foundations to `/setika/auth/login`, `/hrms/branding/resolve`, `/hrms/dashboard/employee`, and `/hrms/attendances/punch` with legacy endpoint fallbacks during rollout. Research outcome: current mobile HRMS expectations center on one tenant-aware API contract, long-lived secure sessions, environment-specific builds, permission-driven navigation, consistent backend validation messages, and safe compatibility adapters while old employee self-service endpoints are phased out. Checks: `flutter analyze`, `flutter test`. | Refactored Flutter core auth/session/API handling, preserved the existing employee login/dashboard flow, mapped the new employee dashboard payload into existing mobile cards, made attendance check-in/out location payloads post to the new HRMS punch endpoint first, and kept legacy adapters for old employee service APIs until HRMS-074I completes feature-by-feature parity. | `setika_hrms_flutter/lib/src/core`, `login.html`, `dashboard.html` |
| HRMS-074I | Flutter employee self-service parity for new HRMS | Complete | Migrated Flutter employee self-service repositories to current `/hrms` services with tolerant new-response mappers and legacy fallback only for missing-route rollout gaps: employee dashboard aggregate, attendance check-in/out with mandatory location/device payload, forgot-punch as `missed_punch` attendance requests, attendance locations, employee attendance device mappings, leave types/balances/history/ledger/apply/cancel, holidays, celebrations, policies, salary-slip list from employee dashboard, salary-slip PDF download, employee `/employees/me` profile/documents/onboarding aggregate, notifications inbox/page/delete, unread count, and push device-token registration. Research outcome: mobile ESS expectations from comparable HRMS products emphasize quick attendance with location/device proof, leave and regularisation self-service, payslip/document access, profile/onboarding completion, push notifications, and compact employee-first navigation; Setika adopts those essentials through the unified tenant-aware `/hrms` contract while intentionally keeping heavier manager/admin action queues for HRMS-074J. Checks: `flutter analyze`, `flutter test`. | Updated Flutter data models/repositories for snake_case HRMS responses while preserving existing screens, loading states, file open/download behavior, and employee dashboard cards. Added mobile-ready attendance location/device DTOs for upcoming prompts and diagnostics, reused existing profile/document and notification UI with new mappers, and kept backend validation messages flowing through the shared API error parser. | `employee-dashboard.html`, `attendance-employee.html`, `leaves-employee.html`, `payslip.html` |
| HRMS-074J | Flutter manager, HR and tenant-admin mobile task center | Complete | Added a permission-driven Flutter task center for managers, HR, and tenant admins while keeping the employee app shell unchanged for normal employees. The mobile session now normalizes both module-local and legacy `hrms.` permission keys, and the dashboard shows a Tasks tab only when identity permissions allow leave approval, attendance request review, HR dashboard view, employee lookup/document review, or candidate listing. The new task center loads independent queues for leave approvals, attendance regularisation approvals, HR dashboard summaries, action notifications, employee lookup/profile quick view, pending employee document review, onboarding document status, payroll readiness counts, attendance exceptions, and recruitment candidates; failed or not-yet-deployed optional endpoints degrade to partial data instead of blanking the screen. Approval comments and document review actions open in dialogs/bottom sheets to avoid clutter. Research outcome: comparable mobile HRMS products such as Zoho People emphasize manager approval actions, attendance/leave workflows, employee directory/profile access, mobile attendance/location support, onboarding documents, analytics, and remote/field employee support; Setika adopts that expected daily-operations companion pattern but keeps heavy setup/reporting web-first and gates everything by tenant permissions. Checks: `flutter analyze`, `flutter test`. | Added `task_center` repository and screen, wired the dashboard Tasks tab behind `session.canOpenTaskCenter`, added approval/reject/comment actions for leave and attendance, employee profile bottom sheet with document review actions, HR dashboard metric cards, action notification and recruitment lists, and mobile-safe permission helpers. | `dashboard.html`, `approval-settings.html`, `leaves.html`, `attendance-report.html`, `pipeline.html` |
| HRMS-074K | Flutter Android and iOS compatibility matrix | Complete | Defined the supported mobile matrix in `QA_TEST_CHECKLIST.md`: Android 7.0/API 24+ with targetSdk 36, iOS/iPadOS 15.0+, Android tablets API 24+, and iPadOS 15+. Confirmed the local project constraints: Android package id `com.citual.setikahrms`, merged debug manifest minSdk 24 and targetSdk 36, Android location/network/internet/notification permissions, iOS deployment target 15.0 in `Podfile` and Xcode project, iOS release bundle id `com.citual.setikahrms`, and iOS location/photo-library usage descriptions. Older Android API 23/iOS 14 devices are explicitly unsupported for the main app and require a separate legacy/no-Firebase dependency strategy if business support is needed. Research outcome: current Flutter platform support is Android API 24-36 and iOS 13-26, Google Play requires new apps/updates to target Android 15/API 35+ as of August 31, 2025, and Firebase Apple setup requires iOS 15+; Setika therefore keeps Android API 24+ and iOS 15+ as the practical support floor while testing latest-target behavior. Checks: `flutter analyze`, `flutter test`. | Expanded the mobile QA checklist with platform decision, project constraint audit, Android API 24/26/29/33/36 and iOS 15/16/17/18+ coverage, phone/tablet/iPad form-factor matrix, permission-denied and poor-network cases, push/background notification checks, secure storage restart checks, file download/open/share checks, gallery upload, HR/admin visibility checks, app upgrade/reinstall, low-memory resume, and future biometric/secure-unlock exit criteria. | `QA_TEST_CHECKLIST.md`, `AndroidManifest.xml`, `Info.plist` |
| HRMS-074L | Flutter mobile release, observability and security hardening | Complete | Added mobile CI workflow for `flutter pub get`, Dart formatting, `flutter analyze`, `flutter test`, Android debug APK, Android debug app bundle, and iOS debug no-codesign build. Added `MOBILE_RELEASE_HARDENING.md` with Android Play App Signing/upload-key handling, iOS Release signing/TestFlight gates, Firebase/APNS production setup, Crashlytics/Analytics activation rules, privacy-safe journey analytics, minimum/forced-version policy, token/support diagnostics rules, root/jailbreak tenant policy, privacy/consent copy, and release checklist. Added privacy-safe `MobileDiagnostics` and wired Flutter framework errors, unhandled zone errors, and Dio API failures through redacted logs; sensitive fields include tokens, passwords, OTPs, coordinates, email/phone, statutory ids, and bank fields. Existing `.gitignore` coverage already excludes build outputs, Pods/DerivedData, signing files, symbols, maps, and Android keystores. Research outcome: market-ready HRMS mobile apps are expected to ship through gated CI, signed app bundles/TestFlight, internal/beta testing, crash and API failure telemetry, privacy-safe logs, APNS/FCM validation, forced-update controls, store privacy declarations, and clear employee consent for location/document flows; Setika adopts those gates now while keeping Crashlytics/Analytics forwarding disabled until production Firebase apps/APNS and consent copy are approved. Checks: `dart format lib/main.dart lib/src/core/network/api_client.dart lib/src/core/observability/mobile_diagnostics.dart`, `flutter analyze`, `flutter test`, `flutter build apk --debug`, `flutter build appbundle --debug`, `flutter build ios --debug --no-codesign`. | Expanded `QA_TEST_CHECKLIST.md` release readiness for workflow gates, signing, store declarations, internal testing, TestFlight, Firebase/APNS, redacted diagnostics, forced-version endpoint, support diagnostics, and tenant root/jailbreak policy. Updated `FIREBASE_MESSAGING_NOTES.md` with production iOS plist/APNS, push behavior, payload privacy, token lifecycle, Crashlytics/Analytics consent gates, and distribution path. Workspace note: the Flutter app folder is currently outside the `spur-hrms` Git repository, so the workflow is colocated at `setika_hrms_flutter/.github/workflows/setika-hrms-flutter-mobile-ci.yml` and is ready to travel with the mobile app repository root. | `QA_TEST_CHECKLIST.md`, `FIREBASE_MESSAGING_NOTES.md`, `MOBILE_RELEASE_HARDENING.md`, `mobile_diagnostics.dart`, `api_client.dart`, `main.dart`, `setika_hrms_flutter/.github/workflows/setika-hrms-flutter-mobile-ci.yml` |
| HRMS-074M | Flutter HR and tenant-admin daily operations companion | Complete | Extended the existing permission-gated Flutter task center into a mobile daily-operations companion without adding new backend tables or duplicating desktop setup screens. Added `notifications.send` / legacy `hrms.notifications.send` session gating, branch/department filter helpers, active employee and probation-due computed lists, readiness counters, attendance exception counts, and a guarded announcement action that reuses `/hrms/notifications/send` with `generalnotification` for in-app/push broadcast to selected employees. Research outcome: comparable HRMS mobile apps emphasize manager/HR approvals, attendance and leave exceptions, field/mobile attendance visibility, employee directory lookup, onboarding/document status, alerts, and lightweight announcements; Setika adopts those high-frequency operations in mobile while intentionally keeping salary templates, pay cycles, permissions, statutory setup, and complex reporting web-first. Checks: `dart format lib/src/core/storage/token_store.dart lib/src/features/task_center/data/task_center_repository.dart lib/src/features/task_center/presentation/task_center_screen.dart`, `flutter analyze`, `flutter test`, `flutter build apk --debug`. | Added concise task-center tabs for Today, Approvals, People, Alerts, Readiness, and Hiring. People now supports compact branch/department bottom-sheet filters. Alerts shows operational/attendance alerts plus action notifications and a modal announcement composer with audience filtering, recipient count, and info-button guidance to avoid sensitive personal/payroll data. Readiness summarizes leave, attendance, onboarding document, payroll blockers, and probation-due employees in dense cards/chips rather than desktop tables. | `dashboard.html`, `employees.html`, `attendance-report.html`, `leave-report.html`, `notification-settings.html` |

## Modern HRMS v2 Backlog

Source of truth: `MODERN_HRMS_REFERENCE.md` and `MODERN_HRMS_TRANSFORMATION.md`.

Do not start HRMS-076 onward until HRMS-075 confirms the traditional HRMS passes real-world tenant, HR, payroll, employee, and admin workflows. Implement modern modules inside the existing `spur-hrms` hexagonal structure; do not create a separate monolith or nested `modules/*` app layout.

| ID | Feature Slice | Status | Backend Scope | Frontend/UI Scope | Tailwind Reference |
|---|---|---|---|---|---|
| HRMS-075 | Traditional HRMS real-world validation gate | Complete | Completed code-level validation gate for this reporting/attendance slice with sqlc generation, full Go test suite, permission manifest updates, route wiring, payroll lock blocker validation, and reusable branded report export verification through compile/build. Remaining production smoke against a live tenant should still be run before modern HRMS work is deployed. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Completed frontend build validation for Attendance, Reports, Leave Reports, and Payroll Operations export flows. Checks: `npm run build`. | Existing implemented HRMS screens |
| HRMS-076 | Modern module guardrails and migration plan | Complete | Added `MODERN_HRMS_IMPLEMENTATION_GUARDRAILS.md` to convert `../MODERN_HRMS_REFERENCE.md` into this repo's architecture: no nested `modules/*`, modern migrations start at `0055`, HRMS-owned tables stay under `hrms`, sqlc query ownership is mapped by aggregate, repository/service/handler files are assigned per HRMS-077 onward slice, route conventions stay under `/hrms` and `/hrms/tenants/{tenantID}`, permission names remain module-local, and AI/integration/privacy rules require ports, auditability, deterministic fallback, and human approval for source-of-truth mutations. Added an `AGENTS.md` pointer so future modern tasks use this guardrails document before the broad reference. Noted that `MODERN_HRMS_TRANSFORMATION.md` is referenced by the backlog but is absent in the workspace, so this guardrail doc becomes the repo-local implementation bridge until that file exists. Research outcome: modern HCM market direction favors one consolidated workforce platform with employee plus contingent worker coverage, skills-based planning, internal mobility, flexible worker payment, privacy-aware analytics, and AI/automation under human control; Setika adopts those expectations through the existing tenant-scoped HRMS module instead of separate worker identities, point-tool modules, or AI-managed source-of-truth writes. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | No modern navigation placeholders were added because HRMS-076 is a guardrail-only task and the backend manifest/scope for HRMS-077+ screens is not implemented yet. Frontend rules are captured in the guardrails: no static permission lists, create/edit forms in modals, helper text behind info controls, web-first setup, mobile-only quick actions. | `MODERN_HRMS_IMPLEMENTATION_GUARDRAILS.md`, `AGENTS.md`, `TASKLIST.md` |
| HRMS-077 | Workforce type taxonomy and classification | Complete | Added tenant-scoped `hrms.worker_types` and `hrms.worker_classification_rules` in `0055_worker_type_taxonomy.sql`, sqlc queries, domain validation, repository/mappers, service lazy seeding for the default India taxonomy, default classification-rule seeding, tenant/super-admin handlers and routes, and `worker_types.*` / `worker_classification_rules.*` manifest permissions. Research outcome: modern HCM products are expected to classify employees and contingent workers in one workforce taxonomy with attendance mode, pay mode, statutory defaults, CLRA/TDS/PF/ESIC/PT flags, agreement/invoice controls, and editable classification guidance; Setika implements those tenant defaults now while worker profile assignment remains scoped to HRMS-078. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Workforce Types settings UI with tenant picker, summary tiles, search/group filters, compliance/pay/attendance/control summaries, create/edit/deactivate modals, and classification-rule list/create/edit/deactivate modal using info-button helper text. Checks: `npm run build`. | Modern settings/workforce screens |
| HRMS-078 | Workforce Hub worker profile extension | Complete | Added tenant-scoped `hrms.worker_profiles` in `0056_worker_profile_extensions.sql` with optional employee/user linkage, independent external worker support, lifecycle/compliance/payroll statuses, org placement, metadata, RLS, sqlc queries, domain validation, ports, repository/mappers, service enrichment from employee profiles when linked, tenant/super-admin handlers/routes, and `workers.*` manifest permissions. Research outcome: current workforce-management products emphasize a single total-workforce view that includes salaried, hourly, project-based, and contingent workers, links time/labor data to payroll and compliance, and uses optional integrations rather than forcing every worker into a standard employee record; Setika adopts that via a separate worker profile aggregate linked to employees only where applicable. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Workforce Hub dashboard UI with super-admin tenant picker, summary tiles, search/type/classification/status filters, worker table, employee-profile link action, and create/edit/deactivate modal forms with helper text behind info buttons. Checks: `npm run build`. | Employee/profile screens |
| HRMS-079 | Engagement engine foundation | Complete | Added tenant-scoped `hrms.engagements` in `0057_engagements.sql` for worker commitments with type, dates, hours budget, rate, currency, org/project/cost-center placement, renewal, termination, metadata, RLS, sqlc queries, domain validation, ports, repository/mappers, service validation against worker profile/branch/department, tenant/super-admin handlers/routes, and `engagements.*` manifest permissions. Research outcome: modern HCM/workforce systems are expected to manage employees, hourly, project, retainer, agency, and contingent commitments as lifecycle records connected to the total worker profile, with budget/rate/renewal/completion status captured for payroll, compliance, and planning; Setika now models the commitment separately from worker identity so old employee records and new non-employee work can coexist. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/.gomodcache go test ./...`. | Added Engagements dashboard UI with super-admin tenant picker, summary tiles, search/type/status/department filters, worker selector, create/edit modal, JSON metadata validation, budget/rate fields, renewal indicators, status transition modal, and deactivate action with helper text behind info buttons. Checks: `npm run build`. | New engagement screens |
| HRMS-080 | Work logs for hourly and contractor work | Complete | Added tenant-scoped `hrms.work_logs` in `0058_work_logs.sql` with daily worker/engagement uniqueness, status/hour constraints, RLS, sqlc queries in `engagements.sql`, domain validation, focused ports, repository/mappers, service workflow rules, handlers/routes, budget validation against submitted plus approved engagement hours, and rollups for payroll/analytics. Research outcome: modern workforce/time-labor products treat hourly and contractor timesheets as approval-controlled records linked to engagements/projects/cost centers, with budget visibility before payroll; Setika adopts submitted/approved-hour budget protection and rollups now while avoiding direct payroll mutation until HRMS-084. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/.gomodcache go test ./...`. | Added Work Logs UI with super-admin tenant picker, summary tiles, logs/approvals/rollups tabs, filters, create/edit modal, submit action, approve/reject review modal, budget-aware error surface, and JSON metadata validation with helper text behind info buttons. Checks: `npm run build`. | Attendance/work-log screens |
| HRMS-081 | Projects and milestones | Complete | Added tenant-scoped `hrms.projects`, `hrms.project_milestones`, and append-only `hrms.project_milestone_events` in `0059_projects_milestones.sql` with budgets, PM/department/branch placement, due dates, acceptance criteria, payment trigger JSON, RLS, sqlc queries in `projects.sql`, domain validation, focused ports, repository/mappers, service workflows, handlers/routes, and `projects.*` plus `milestones.*` manifest permissions. Research outcome: modern project/SOW workforce systems manage delivery as projects with budget ownership and milestone/deliverable acceptance before payment readiness; Setika adopts budget and acceptance controls plus audit metadata now while intentionally avoiding automatic payroll mutation until the flexible payroll slice. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/.gomodcache go test ./...`. | Added Projects dashboard UI with super-admin tenant picker, summary tiles, project/milestone/board tabs, filters, project and milestone modals, engagement links, acceptance controls, budget/burn summaries, JSON metadata validation, and helper text behind info buttons. Checks: `npm run build`. | Project/milestone screens |
| HRMS-082 | Generic agreements and SOW lifecycle | Needs Rework | Current agreement/template/PDF foundation exists, but the slice was framed too narrowly around simple signature tokens and future India e-sign. Rework must pivot to a Document Sign workflow model like Zoho Sign/DocuSign: document request templates, configurable workflow definitions, ordered/parallel steps, actors/roles, request initiator rules, approval steps, signer steps, review/comment/recall/reject actions, reminders, audit trail, tenant branding, and HRMS agreement linkage. Do not assume Aadhaar/DSC/e-sign; most employees will use normal document-sign acceptance with typed/drawn/uploaded signature or approval attestations. Research correction: Zoho Sign emphasizes templates, signing order, document status tracking, reminders/recall, approval/deny/decline controls, branding, and audit trails; Setika must adopt workflow-first document signing instead of provider-first statutory e-sign. | Existing Agreements UI is only a starting point. Rework UI must expose workflow configuration and request execution: who can generate, who approves, who signs, step sequence, escalation/reminders, document preview, comments, recall/reject, audit trail, and tenant-branded signed output. | Zoho Sign / DocuSign style workflow screens |
| HRMS-083 | Reusable Document Sign workflow module | Complete | Added a separate reusable `spur-document-sign` module with tenant-scoped MinIO/S3-compatible storage settings, sqlc migrations/queries, PostgreSQL repository, object-storage adapter, workflow service, templates, workflow definitions/steps, request/step instances, source/signed/audit/attachment artifacts, approve/review/sign/reject/comment/recall actions, audit events, module manifest permissions, embedded migrations, and Setika host registration/routes at `/document-sign`. Tenant storage can use platform defaults or tenant-owned MinIO/S3-compatible settings. Research outcome: Zoho Sign and DocuSign style products expect template libraries, ordered/parallel workflow routing, signer/approver roles, comments, reminders/recall/reject paths, branding/auditability, and reusable request tracking; Setika implements workflow-first document signing and keeps statutory e-sign provider integrations optional. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-document-sign/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-document-sign/.gomodcache go test ./...`, `GOCACHE=/Users/dinesh/workplace/setika-new/setika/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-document-sign/.gomodcache go test ./...`. | Added a Document Sign workspace in the dashboard for storage settings, workflow builder, template library, request inbox/details, step actions, artifact upload, and audit timeline. Forms open in modal popups and helper guidance is behind info buttons. Checks: `npm run build`. | `document-sign` workspace |
| HRMS-083B | Optional statutory/e-sign provider adapters | Complete | Added optional provider infrastructure to `spur-document-sign` with tenant-scoped Digio/Leegality/DSC/Aadhaar/custom provider settings, provider workflow-step links, provider request tracking, public HMAC webhook sync, signer/provider status updates, audit webhook event capture, retry scheduling fields, signed/audit certificate URL capture in provider metadata, provider permissions, and default adapter hooks that can be replaced by provider-specific implementations. Normal employee signing remains available without provider configuration. Research outcome: market e-sign products separate template/workflow signing from statutory provider rails, require tenant/provider credentials, signed document and audit certificate evidence, webhook-driven status sync, HMAC verification, retry paths, and fallback to normal signing when statutory rails are not required or provider services are unavailable. Checks: `sqlc generate`, `env GOCACHE=/private/tmp/setika-docsign-gocache go test ./...`, `env GOCACHE=/private/tmp/setika-host-gocache go test ./...`. | Added a Providers tab for provider credentials/tests and workflow-step provider mapping, plus provider-backed status and start controls inside Document Sign request progress. Forms open in modal popups and helper text stays behind info buttons. Checks: `npm run build`. | Integration/settings screens |
| HRMS-084 | Flexible payroll for non-employee workers | Complete | Added tenant-scoped flexible pay runs, contractor invoices, pay run items, and audit events in `0062_flexible_worker_payroll.sql`, sqlc queries in `flex_payroll.sql`, domain validation, ports, repository/mappers, service generation from approved work logs and accepted milestones, manual retainer/stipend/invoice/adjustment items, TDS 194C/194J and GST calculations, approval/payment lifecycle, CSV payroll export, routes, and manifest permissions. Research outcome: current HR/payroll products for contractors expect invoice-backed approvals, source-linked timesheet/milestone payables, statutory tax capture, payment status, and finance export handoff; Setika adopts those controls while keeping employee payroll separate and avoiding automatic bank/payment-provider mutation. Checks: `sqlc generate`, `go test ./...`. | Added Flexible Payroll dashboard screen with super-admin tenant picker, run summary, pay run creation, contractor invoice modal, source/manual item review, TDS/GST/net summaries, approval/payment actions, audit timeline, and CSV download using shared API download helpers. Forms open in modal popups and helper guidance is behind info buttons. Checks: `npm run build`. | Payroll screens |
| HRMS-085 | Compliance rules and checklist engine | Complete | Added tenant-scoped compliance rules, worker/engagement checklist items, and compliance audit events in migration `0063_compliance_checklists.sql`, sqlc queries, domain validation, default India workforce compliance presets, repository/service/handler/routes, rule applicability by worker, engagement, classification, worker type, engagement type, branch, and department, checklist generation, overdue refresh, evidence metadata, review/waiver lifecycle, payroll blocker flags, summary API, and module permissions. Research outcome: comparable HRMS/compliance tools keep compliance easy through configurable rules, generated checklists, evidence/expiry signals, waivers, audit trails, and exception-focused dashboards instead of legal text-heavy pages, so Setika uses tenant-owned rule values with sensible presets. Checks: `sqlc generate`, `go test ./...`. | Added Compliance dashboard with super-admin tenant picker, KPI cards, rules/checklist/audit tabs, default seeding, checklist generation, rule modal, evidence/reference modal, review modal, waiver modal, payroll-blocker indicators, and helper text behind info buttons. Checks: `npm run build`. | Compliance/dashboard screens |
| HRMS-086 | Skills catalog and worker skills | Complete | Added global plus tenant skill categories, seeded global skill catalog, tenant skill CRUD, worker skills, proficiency, years, certificate URL/expiry, assessment score/date, manager endorsement, HR verification/rejection/expiry, worker skill assessments, search/filter APIs, summary API, RLS migration `0064_skills_catalog.sql`, sqlc queries, domain validation, repository/service/handler/routes, and module permissions. Research outcome: current HCM skills products such as Workday Skills Cloud and SAP talent capabilities use a managed skills foundation connected to employee profiles, learning, mobility, hiring, and analytics; Setika implements the foundation as a searchable catalog plus worker evidence/verification records so HRMS-087 and HRMS-088 can reuse normalized skill IDs instead of free-text tags. Checks: `sqlc generate`, `go test ./...`. | Added Skills dashboard with super-admin tenant picker, KPI cards, Worker Skills/Catalog/Assessments tabs, search/category/status filters, catalog category/skill modals, worker-skill modal, manager endorsement/HR verification modal, assessment modal, certificate links, and helper text behind info buttons. Checks: `npm run build`. | Profile/settings screens |
| HRMS-087 | Project skill requirements and skill gaps | Complete | Added project/engagement skill requirements with migration `0065_project_skill_requirements.sql`, sqlc queries, domain validation, repository/service/handler/routes, module permissions, gap scoring, project/org summaries, and single-person dependency detection. Gap scoring counts only manager-endorsed or HR-verified worker skills that meet required proficiency and years, and assigned coverage is resolved through engagement, project milestone, project code, or project label links. Research outcome: current talent platforms position project staffing around skills foundations, coverage scoring, missing capability analysis, and build/buy recommendations; Setika implements this as auditable deterministic scoring first so HR can trust the numbers before adding AI recommendations. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Skill Gaps dashboard with super-admin tenant picker, summary/requirements/gaps/dependencies tabs, project/importance/search filters, KPI cards, requirement create/edit modal, requirement delete action, match percentages, missing skills, suggested train/assign/hire actions, and dependency risk view. Checks: `npm run build`. | Analytics/project screens |
| HRMS-088 | Internal talent marketplace | Complete | Added tenant-scoped marketplace opportunities, applications, and event history in migration `0066_talent_marketplace.sql`, sqlc queries, domain validation, repository/service/handler/routes, candidate fallback status, worker accept/decline/application lifecycle, match-score storage, skills-based worker recommendation scoring from project/engagement requirements, and module permissions. Research outcome: current internal talent marketplace products emphasize skills-based opportunity discovery, project/gig/role matching, worker choice, manager governance, candidate fallback to hiring, and auditable mobility actions; Setika implements that deterministic workflow first so HR can trust match reasons and event history before AI automation. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Talent Marketplace dashboard with super-admin tenant picker, opportunity cards, create/edit modal, recommendation tab with worker invite action, application record/status modal, accept/assign/decline actions, candidate fallback modal, event history, filters, KPI cards, and helper text behind info buttons. Checks: `npm run build`. | Marketplace screens |
| HRMS-089 | OKR and continuous performance foundation | Complete | Added tenant-scoped OKR cycles, objectives, key results, and key-result check-ins in migration `0067_okrs.sql`, sqlc queries in `performance.sql`, domain validation, owner-type rules, key-result progress calculation, objective progress rollup refresh, status lifecycle APIs, summary rollups, repository/service/handler/routes, and module permissions. Research outcome: current OKR/performance tools expect time-bound cycles, aligned company/team/individual objectives, measurable key results, visible confidence/status, and regular progress check-ins instead of annual-only appraisal records; Setika implements that continuous deterministic foundation now so HRMS-090 feedback can build on auditable goals. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added OKR Performance dashboard with super-admin tenant picker, KPI cards, cycle/objective/key-result/check-in tabs, filters, cycle/objective/key-result modals, key-result check-in modal, cycle activation/closure actions, hierarchy/owner visibility, rollup progress, and helper text behind info buttons. Checks: `npm run build`. | Performance/OKR screens |
| HRMS-090 | Performance check-ins and feedback | Complete | Added tenant-scoped weekly performance check-ins, mood/highlights/blockers/next-plan capture, submit/review lifecycle, manager scoring, calibration buckets, 360 feedback requests/responses, anonymity and visibility policies, performance timeline events, summary rollups, calibration-ready extraction, sqlc queries in `performance.sql`, migration `0068_performance_checkins_feedback.sql`, domain validation, repository/service/handler/routes, and module permissions. Research outcome: current continuous-performance HRMS patterns expect frequent lightweight check-ins, structured manager review, 360 feedback with anonymity controls, visibility policies, timeline history, and calibration exports connected to goals rather than annual-only appraisal records; Setika implements that workflow around OKR cycles and worker profiles. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Performance dashboard with super-admin tenant picker, KPI cards, cycle/worker/status filters, Check-ins/Feedback/Timeline/Calibration tabs, modal-only check-in/review/feedback request/feedback response forms, helper guidance behind an info button, anonymity-aware feedback response display, and dashboard navigation for tenant, employee, and manager contexts. Checks: `npm run build`. | Performance screens |
| HRMS-091 | Wellbeing and pulse surveys | Complete | Added tenant-scoped pulse/wellbeing/engagement surveys, questions, responses, wellbeing scores, HR-only alerts, consent capture, anonymity thresholds, manager aggregate-only visibility controls, critical response alerting, aggregate suppression below threshold, sqlc queries in `wellbeing.sql`, migration `0069_pulse_wellbeing.sql`, domain validation, repository/service/handler/routes, and module permissions. Research outcome: current pulse/wellbeing HR products expect short recurring surveys, explicit consent for sensitive signals, anonymous employee responses, small-group suppression, manager aggregate views, and HR-only escalation for critical risk; Setika implements those controls first so wellbeing data is useful without exposing individual sensitive data broadly. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Wellbeing dashboard with super-admin tenant picker, KPI cards, survey/question/response/self-score/alert/aggregate tabs, modal-only survey/question/response/score/alert forms, filters, aggregate suppression indicators, anonymous respondent display, HR alert review, and dashboard navigation for tenant, employee, and manager contexts. Checks: `npm run build`. | Wellbeing screens |
| HRMS-092 | AI action layer foundation | Complete | Added AI action foundation in `0070_ai_action_layer.sql`, `ai_action_layer.sql`, generated sqlc, domain models, repository/mappers, service ports, service methods, handlers/routes, and module permissions. The layer now stores AI signal logs, agent action proposals, human overrides, and event outbox rows with severity, confidence, visibility scope, input snapshot, explanation, proposed action, human review, and failure/audit fields. Insight refresh now records signals/actions/outbox events, and dismissed/overridden insights create human override records. Added event publisher and Python sidecar ports so Redis/event-bus and AI sidecar integrations can be wired without changing HR workflows. Research/scenario outcome: modern HR AI is moving toward agent/copilot recommendations, but enterprise HR needs explainability, privacy-scoped visibility, source signals, audit trails, and human approval/override before decisions affect employees; Setika implements this as a review-first action layer for anomaly insights, workflow events, sidecar scoring, manager-safe aggregation, HR-only sensitive signals, and admin-level event operations. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Rebuilt the dashboard Insights screen into an AI Action Console with tenant-aware loading, metrics, severity/status/category/visibility filters, tabs for insights/actions/signals/overrides/event outbox, explain-why panel, audit trail, action status modal, human override modal, employee-safe visibility labels, and helper text behind info buttons. Checks: `npm run build`. | AI/analytics screens |
| HRMS-093 | Bounded AI agents | Complete | Added bounded AI agent definitions and run workflow behind service ports for leave intelligence, payroll anomaly, attrition risk, engagement health, compliance guard, and onboarding autopilot. Each run writes deterministic fallback recommendations into the existing AI action log with guardrails, input snapshot, explainability, confidence, model version, and human-review requirement; sidecar wiring remains available through HRMS-092 ports. Added tenant and super-admin APIs under `/ai/agents` and `/ai/agents/run`, service methods, handlers/routes, and module permissions. Research outcome: enterprise HR AI should act as bounded copilots/agents that propose next actions inside existing workflows, never silently approve leave, change payroll, expose sensitive attrition/wellbeing signals, or waive compliance; Setika adopts deterministic, auditable, human-approved recommendations first. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added bounded agents tab to the AI Action Console with cards for workflow, signal sources, guardrails, severity/visibility, run-one/run-all controls, and shared action-review workflow for the created recommendations. Checks: `npm run build`. | Existing workflow screens |
| HRMS-094 | Modern people analytics dashboards | Complete | Added sqlc-owned aggregate query `people_analytics.sql`, repository/service/handler/routes, service port, and permission for a modern people analytics workspace covering workforce type mix, contract renewals, project health, skill coverage, wellbeing trends, payroll cost intelligence, and risk heatmaps. Research outcome: HR analytics products increasingly combine workforce planning, skill intelligence, engagement/wellbeing aggregates, cost visibility, and predictive risk indicators; Setika keeps this aggregate-first and avoids exposing individual sensitive wellbeing/attrition data on analytics dashboards. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added People Analytics dashboard with super-admin tenant picker, KPI cards, tabs for overview/workforce/skills/wellbeing/cost/risks, workforce mix cards, risk heatmap, and dashboard navigation for HR/manager analytics permissions. Checks: `npm run build`. | Dashboard/analytics screens |
| HRMS-095 | Ecosystem and privacy hardening | Complete | Added migration `0071_privacy_ecosystem.sql`, sqlc queries in `privacy_ecosystem.sql`, domain/ports/repository/service/handler/routes, and permissions for privacy consents, data erasure/export/correction/restriction workflows, WhatsApp/Slack/email/Git/webhook/API integration hooks, and mobile API workflow constraints for low-bandwidth, offline, location, device binding, and version compatibility. Research outcome: DPDPA-style HRMS hardening needs consent evidence, auditable data-subject request workflows, integration hooks with consent controls, and mobile-safe API constraints because employee workflows often run on older phones and low-bandwidth networks; Setika adds tenant-level controls without hard-coding providers. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Privacy & Ecosystem dashboard with super-admin tenant picker, metrics, tabs for consents/erasure/integrations/mobile constraints, modal-only create/status flows, info-button guidance, and dashboard navigation. Checks: `npm run build`. | Settings/mobile screens |
| HRMS-096 | P0 - HR Operations Workbench | Complete | Added a query-only unified operations workbench over existing source workflows without duplicating approval/business logic: sqlc query `operations_workbench.sql`, generated code, domain/port/repository/mapper/service/handler/routes, tenant and super-admin endpoints, priority/severity/due-date/action metadata, SLA-style counts, and manifest permission `operations_workbench.view`. Aggregates pending leave approvals, attendance exceptions and payroll blockers, employee exits, candidate onboarding tasks, employee letters, agreements/document sign items, compliance checklist blockers, insights, and AI action recommendations. Employee helpdesk lane is represented in summary but will populate after HRMS-097 creates case records. Research outcome: approval/workflow products centralize received/sent action queues with direct source-record actions and completed-state separation; Setika adopts a read-first workbench that routes to owning modules now and leaves full workflow persistence to HRMS-106. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Operations Workbench dashboard section with tenant picker for super-admin, KPI tiles, tabs for Today/Approvals/Exceptions/Payroll Blockers/Joining/Exit/Compliance/Employee Requests/AI, severity/search filters, queue list, detail pane, metadata facts, info-button helper text, and route actions into the owning module screens. Wired navigation under Tenant Operations, People, and My Team. Checks: `npm run build`. | `dashboard.html`, `employee-dashboard.html`, operational queue screens |
| HRMS-097 | P0 - Employee Helpdesk and HR Case Management | Complete | Added tenant-scoped HR case management with categories, SLA policies, confidentiality levels, requester/subject/owner routing, status lifecycle, comments, object-storage-backed attachments, event audit trail, tenant and super-admin routes, sqlc queries/mappers/repository/service/handler wiring, storage category `hr-cases`, and module permissions for list/create/view/update/assign/status/comment/attach/restricted-view/category/SLA management. Integrated open HR cases into Operations Workbench as the `employee_requests` lane. Research outcome: modern HR service delivery products separate employee intake from HR operator queues, with request categories, SLA clocks, ownership, private/internal comments, attachments, escalation, audit history, and restricted visibility for sensitive/grievance cases. Setika implements that as a tenant-configurable helpdesk layer instead of one-off request screens. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added HR Helpdesk dashboard section with super-admin tenant picker, case queue/detail workspace, KPI tiles, status/priority/search filters, categories and SLA tabs, info-button helper text, and modal-based forms for new case, category, SLA policy, status change, assignment, comments, and attachments. Wired navigation under Tenant Operations, My Work, and My Team, and workbench route target `hr-helpdesk`. Checks: `npm run build`. | Support/helpdesk screens, `tickets.html` if available |
| HRMS-098 | P1 - Learning, Training, and AI Upskilling | Complete | Implemented native learning tables, sqlc queries, domain/ports/service/repository/handlers/routes, tenant MinIO/S3 certificate storage, learning permissions, and role template coverage. Market review: modern HR learning products emphasize skills-linked catalogs, mandatory/compliance courses, learning paths, assignment/completion tracking, certificates, and AI/skill-gap recommendations without forcing an external LMS; Setika now supports that model natively while leaving provider/storage configurable per tenant. Verification: `go test ./...` passed in `spur-hrms`; `npm run build` passed in `frontend`. | Added Learning dashboard with tenant picker, catalog, paths, assignments, recommendations, skill/worker selectors, summary KPIs, modal-only forms, info-button guidance, certificate upload, recommendation generation, and employee/manager/admin nav entries. | Learning/training screens |
| HRMS-099 | P1 - Compensation Review, Pay Bands, and Pay Equity | Complete | Added tenant-scoped compensation pay bands, review cycles, budget pools, employee compensation recommendations, compa-ratio fields, equity checks, status/audit events, payroll-handoff status controls, sqlc queries, domain validation, repository/service/handler/routes, and module permissions. Salary slip generation and employee salary master mutation remain separate; recommendations can be finalized/handed to payroll but do not silently alter payroll records. Research outcome: SAP SuccessFactors, Lattice, and broader compensation-planning products emphasize pay bands, structured review cycles, budget guardrails, pay-for-performance context, fairness/equity analytics, auditability, and clear employee communication; Setika adopts the planning/equity/control layer first and avoids automatic payroll mutation until workflow approvals are fully centralized. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Compensation Review dashboard with super-admin tenant picker, KPI tiles, tabs for cycles/pay bands/budget pools/recommendations/equity/audit, modal-only create/update/status flows, equity-check generation, and dashboard navigation under Payroll and Tenant Operations. Checks: `npm run build`. | Payroll/compensation screens |
| HRMS-100 | P1 - Succession Planning and Critical Role Coverage | Complete | Added tenant-scoped succession review cycles, critical roles, incumbent/emergency-cover mapping, successor nominations, readiness levels, risk/impact ratings, successor development actions linked to learning courses/paths, audit events, summary analytics, sqlc queries, domain validation, repository/service/handler/routes, and module permissions with HR-confidential defaults. Research outcome: modern succession products emphasize critical-position inventories, multiple successor slates, readiness windows, emergency cover, retention/vacancy risk, development plans tied to learning/performance, and regular review cycles; Setika implements that model as a confidential HR workspace without exposing manager/employee access by default. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added Succession Planning dashboard with super-admin tenant picker, KPI tiles, tabs for cycles/critical roles/successors/development actions/heatmap/audit, modal-only create/update/status flows, employee/department/designation/learning selectors, and navigation under People and Tenant Operations. Checks: `npm run build`. | Talent/performance analytics screens |
| HRMS-101 | P1 - Asset and Access Lifecycle | Complete | Backend added tenant-scoped asset inventory, access catalog, asset issue/return records, access provisioning/deprovisioning tasks, onboarding/exit reference fields, damage/loss/recovery states, audit events, summary metrics, RLS policies, sqlc repositories, services, routes, and module permissions. Research outcome: modern HR/IT lifecycle tools combine IT asset management, SaaS/facility access provisioning, onboarding issue tasks, offboarding revocation, return clearance, ownership/custodian accountability, and auditable exception handling; Setika implements this as a unified tenant workspace rather than separate cluttered setup pages. Verification: `sqlc generate`, `go test ./...`, and frontend `npm run build` passed. | Added clean Asset & Access workspace in HR operations with tenant selector, KPI summary, tabs for assets/access/assignments/tasks/clearance/audit, and modal forms for create/edit/status changes. | Onboarding/exit/admin asset screens |
| HRMS-102 | P2 - Shift Planning and Workforce Scheduling | Complete | Implemented tenant-scoped shift templates, staffing requirements, schedule assignments, swap requests, audit events, conflict detection, payroll blockers, staffing gap summaries, attendance policy/location links, permissions, and current-tenant/super-admin APIs. Market research outcome: scheduler UX should focus on reusable templates, publish/lock states, conflict/gap warnings, swap requests, and attendance/payroll readiness rather than a crowded calendar-only screen. | Added clean Shift Planning dashboard with overview metrics, roster/templates/requirements/swaps/gaps tabs, modal create/update flows, info-button guidance, tenant picker, and dashboard navigation. Mobile employee schedule can consume the same assignment/swap APIs. Verified with `go test ./...` and `npm run build`. | `calendar.html`, attendance admin screens |
| HRMS-103 | P2 - Benefits and Claims Administration | Complete | Added benefit plans, enrollment windows, dependents/nominees, enrollments, claim types, claims, attachments through tenant storage port, claim review/payment/payroll export states, limit checks, summary/audit APIs, RLS-backed migration/query/sqlc/repo/service/handler/routes/manifest permissions. Research outcome: HRMS users expect employee self-service enrollment/dependent/claim/evidence upload plus HR approval/payment/payroll readiness; adopted those flows while keeping direct payroll export as status/reference hooks. Verified `sqlc generate`, `go test ./...`, and frontend `npm run build`. | Added Benefits & Claims workspace with tenant picker, summary cards, tabs, and modal forms for plan/window/dependent/enrollment/claim-type/claim/review/payment/attachment workflows. | Benefits/claims/payroll screens |
| HRMS-104 | P0 - Employee Relations, Grievance, and Disciplinary Workflow | Complete | Added confidential ER categories, ER cases, controlled helpdesk source reference, parties, allegations/incidents, investigation steps, witness notes, tenant-storage evidence, findings, action plans, legal hold, immutable audit events, RLS-backed migration/query/sqlc/repo/service/handler/routes, and restricted module permissions. Research outcome: ER platforms and formal investigation guidance separate confidential investigations from helpdesk queues, preserve factual records, restrict witness/evidence visibility, maintain legal hold, and keep append-only audit trails; Setika adopts that pattern without broad HR helpdesk visibility. Verified `sqlc generate`, `go test ./...`, and frontend `npm run build`. | Added confidential Employee Relations workspace with tenant picker, restricted case queue, summary metrics, category management, modal case intake, status/legal-hold modals, investigation artifact modals, evidence upload, findings/action plan workflow, and audit timeline. | HR case management/grievance screens |
| HRMS-105 | P0 - HR Command Center UX Refactor | Complete | Added a read-only HR command center API over the existing Operations Workbench instead of new business tables. The endpoint returns role/workflow navigation metadata, quick actions, source-route targets, and grouped counters/cards for Today, Approvals, Exceptions, Payroll Close, People Movement, Employee Requests, and Insights, with tenant and super-admin tenant-scoped routes plus manifest permission `command_center.view`. Research outcome: ServiceNow, Workday, SAP SuccessFactors, and Zoho-style HR workspaces emphasize personalized work queues, to-do/approval entry points, unified employee experience, workflow automation, and AI recommendations in the operating context; Setika adopts a command-center experience that routes to owning workflows rather than duplicating approval/payroll/business logic. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added HR Command Center landing UI with summary cards, lane selector cards, quick actions, priority queue, in-context AI panel, info-button helper text, tenant picker for super-admin mode, and source workflow navigation. HR/Tenant Admin users now land on the command center instead of the legacy HR analytics dashboard; the legacy HR dashboard remains available as an analytics screen. Checks: `npm run build`. | `dashboard.html`, `employee-dashboard.html`, all workflow entry screens |
| HRMS-106 | P0 - Workflow Task Engine and Operation Templates | Complete | Added reusable tenant-scoped workflow/task engine: migration `0077_workflow_task_engine.sql`, sqlc queries/generated code, domain/ports/service/repository/mappers/handlers/routes, workflow definitions and steps, operation templates, workflow tasks, events, comments, watchers, storage-backed attachments through tenant MinIO/S3 settings, priority/severity/due/visibility fields, delegation/completed/watch states, task summary API, and manifest permissions. Task actions update workflow task state and record source action intent without duplicating leave/payroll/document/tenant/employee business rules; owning-module transaction executors remain the integration point for source-of-truth mutations. Research outcome: enterprise HR workflow products converge on inbox/sent/completed/delegated folders, reading panes, comments, attachments, audit timelines, delegation, due/SLA priority, and source-record actions; Setika adopts that pattern as a reusable task engine while avoiding source-module business logic duplication. Verification: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`, and frontend `npm run build` passed. | Added Outlook-style Workflow Inbox dashboard section with tenant picker, Inbox/My Requests/Team/Watching/Completed/Delegated folders, task list, reading pane, timeline/comments/attachments, modal-only New Task, Approve/Reject/Request Info/Complete, Delegate, Comment, and Attach flows, plus dashboard navigation wiring under Tenant Operations, People, and My Team. | `mail.html`, `tasks.html`, `dashboard.html`, workflow inbox screens |
| HRMS-107 | P0 - Global New Command Launcher and Role-Scoped Request Catalog | Complete | Added a service-owned operation catalog with current-tenant and super-admin tenant-scoped APIs (`/operation-catalog`, `/tenants/{tenantID}/operation-catalog`). Catalog entries define category, label, description, required permissions, target module/section, workflow template key/id, launch mode, approval policy, source service command, priority/severity, assignee hints, launch schema, metadata, sort order, and mobile availability. The catalog combines curated Setika commands for Employee Request, HR Operation, Tenant Operation, Payroll Operation, Document Workflow, Asset/Access Request, Case/Grievance, and Configuration Change with active tenant operation templates. Added manifest permission `operation_catalog.view`; no new business tables were needed. Research outcome: ServiceNow HRSD/Employee Center, Zoho People cases/workflows, Workday-style task launches, and SAP SuccessFactors workflow patterns converge on searchable service/request catalogs that route users to governed source workflows, expose only role-appropriate actions, and keep approvals/audit in the owning module. Setika adopts that model and intentionally avoids duplicating leave/payroll/document/tenant business forms in one oversized launcher. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added a global `New` button in the sticky dashboard header plus a Command Center `New` action. The launcher opens as a clean modal command picker with search, category filter, selected-command detail, permission/approval/source metadata, and primary launch action. It filters commands using the current identity permissions; navigation commands open the owning HRMS screen, and workflow-task commands create a task shell then route to Workflow Inbox. Super-admin users see non-tenant navigation operations until HRMS-108 adds governed tenant-operation workflows. Checks: `npm run build`. | `dashboard.html`, `employee-dashboard.html`, modal command palette/task screens |
| HRMS-108 | P0 - Tenant Operation Governance and Safe Change Workflows | Complete | Added HRMS-owned governed tenant operation requests/events with migration `0081_tenant_operation_governance.sql`, sqlc queries/generated code, domain validation, repository/mappers, service lifecycle, super-admin handlers/routes, manifest permissions, and operation catalog routing. Supports create tenant, suspend/restore, schedule/cancel deletion, module enable/disable, storage change, domain/branding change, admin reassignment, and data export as request records with validation, approval, progress, backup confirmation, retention, rollback metadata, and audit events. Identity/platform-owned tenant mutation remains outside HRMS and is intentionally not bypassed; this queue records safe execution intent until executor services are attached. Research outcome: governed platform/account operations follow change-management/account-factory patterns with permission gates, validation/baseline checks, staged execution, backup/rollback evidence, and audit history; Setika adopts that instead of direct high-risk tenant buttons. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added a super-admin Tenant Operations Governance Queue, reachable from Tenant Operations navigation, global `New` catalog target, and tenant row governance action. The workspace has KPI cards, filters, request queue, detail/audit panel, info-button guidance, and modal-only create/action flows for validate/approve/start/complete/reject/cancel/fail with backup and rollback evidence. The main tenant list now routes new tenant/change work to the governed queue instead of the old direct Add Tenant button. Checks: `npm run build`. | `companies.html`, `company-details.html`, `dashboard.html`, tenant operations screens |
| HRMS-109 | P0 - First-Level Navigation IA Cleanup | Complete | No backend changes. Research outcome: the prior UX audit and comparable HR workspace patterns show that first-level navigation should follow user jobs rather than implementation modules: inbox/work, people records, time, payroll, hiring, performance, reports, and setup. Setika adopts that model for tenant users while keeping super-admin platform navigation separate. | Replaced the tenant dashboard first-level menu with `Home`, `Inbox`, `People`, `Time`, `Payroll`, `Hiring`, `Performance`, `Reports`, and `Setup`. Parent menu rows now expand/collapse without navigating, child rows navigate to modules, and the sidebar styling is compacted toward the Tailwind template pattern with smaller rows, section title, nested line treatment, and active child marker. Checks: `npm run build`. | `dashboard.html`, dashboard shell navigation |
| HRMS-110 | P0 - Main Workspace Inbox Correction | Complete | No backend changes; reused existing current-tenant operations workbench and celebration APIs. Research outcome: Outlook-style work queues belong in the main workspace after a user selects Inbox from primary navigation; the left rail should remain navigation only, while the working area carries the item list, reading pane, filters, and source-workflow actions. | Changed the tenant left menu `Inbox` into a single-click top-level destination. Added a main workspace Inbox with item list and reading pane for celebrations/birthdays/events plus actionable work items such as approvals, offer letters, helpdesk, payroll blockers, exits, and document tasks when returned by the existing APIs. Selecting an item opens its detail in the workspace, and the primary action routes to the owning workflow. Removed the incorrect sidebar activity-feed behavior. Checks: `npm run build`. | `dashboard.html`, dashboard shell navigation |
| HRMS-110A | P0 - Inbox Small-Screen Sample Preview | Complete | No backend changes and no seeded data. | Added guarded sample Inbox items for local/dev visual preview when the real inbox has too few rows, including birthday, offer letter, leave approval, attendance regularisation, payroll blocker, document-sign, exit clearance, and anniversary examples. Real API rows remain first-class, and preview rows supplement the list until there is enough density to judge the small-screen layout. The sample state is labelled `Sample preview`, and spacing/list heights were tightened for phone-width review. Checks: `npm run build`. | `dashboard.html`, small-screen inbox preview |
| HRMS-110B | P0 - Inbox Leave Approval Route Fix | Complete | No backend changes. | Fixed the Leave Approvals screen API base path so Inbox actions route into endpoints under `/hrms` and no longer call missing root paths like `/employees` or `/leave-approvals`. Checks: `npm run build`. | `dashboard.html`, `LeaveApprovalsSection.tsx` |
| HRMS-110C | P0 - Inbox Completion and Completed History | Complete | Reused existing workflow-task completed view and action endpoint; no schema changes. Research outcome: task inboxes should separate active work from completed/archive history so completed work leaves the active queue but remains auditable and searchable in a dedicated history destination. | Added a separate left-menu `Completed` destination instead of keeping Completed inside Inbox. Inbox now shows active work only; Completed shows completed workflow-task history with its own search and filters. Workflow tasks can be completed from the Inbox detail pane via the existing workflow action API and then route to Completed. Local/dev sample tasks can also be completed and move to Completed preview history, while source-owned tasks without generic completion still route to their owning workflow. Checks: `npm run build`. | `dashboard.html`, inbox/completed workspaces |
| HRMS-110D | P0 - Inbox Inline Email-Style Actions | Complete | Reused existing leave approval and workflow task action APIs; no schema changes. Research outcome: enterprise HR task inboxes work best when they behave like email/Teams approval surfaces: the user opens a message, reviews all context, adds a note/reply, and acts inline without being pushed into another module unless deeper record work is needed. | Converted Inbox detail into the action surface. Leave approval workbench rows now expose Approve/Reject directly below the request details with optional remarks and move out of active Inbox after action. Workflow tasks keep inline Complete/Approve/Reject where supported. Celebration/event rows expose Reply and Quick Wish actions from the same reading pane. Completed remains a separate searchable left-menu destination, while source-module navigation is only a secondary full-record fallback. Leave approval workbench metadata now includes employee, department, leave type, dates, day type, days, reason, and applied date so the inbox detail can show full context. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`, `npm run build`. | `dashboard.html`, inbox/completed workspaces |
| HRMS-110E | P0 - Work Item Action Framework | Complete | Added a reusable action contract so Inbox renders module-declared actions instead of hard-coding every HR operation. Research outcome: Zoho People approvals support criteria-based multi-level approvers, comments, forwarding, reminders, and approval lists; ServiceNow HRSD emphasizes workflows, to-do lists, guided recommendations, and case resolution in one employee work surface. Setika adopts action contracts and source-module ownership, not a monolithic Inbox business layer. | Operations Workbench cards now declare allowed actions. Added a tenant-scoped and super-admin tenant-scoped workbench action endpoint that reloads the card server-side, validates the action contract, and delegates supported leave approval approve/reject actions to the Leave service. Inbox now renders generic inline action buttons from `actions`, calls the workbench action endpoint for workbench-owned items, keeps workflow tasks on workflow action APIs, and uses local event actions for celebration replies. Full-record navigation remains a non-inline fallback action. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`, `npm run build`. | `dashboard.html`, inbox/completed workspaces, workbench APIs |
| HRMS-110F | P0 - Workbench Naming and Role-Scoped Visibility | Complete | No schema changes. Reused existing manifest permission `operations_workbench.view`; employee-only sessions must not load tenant-wide HR operations cards after HR access is removed. Research outcome: enterprise HR work queues separate personal work surfaces from privileged HR operations queues and keep task visibility tied to live role/permission context. Setika adopts `Workbench` for the personal action surface and reserves operations feed access for users with the operations workbench permission. | Renamed the first-level `Inbox` destination to `Workbench`, kept `Completed` as a separate history destination, gated `/operations-workbench` loading behind `hrms.operations_workbench.view`, and filtered local/dev HR-only preview cards for employee-only users. Workflow tasks, celebrations, and completed history remain available to employees. Checks: `npm run build`. | `dashboard.html`, workbench/completed workspaces |
| HRMS-111 | P0 - Secure Tenant Subdomain Login | Complete | Made tenant subdomains first-class app origins: added `TENANT_BASE_DOMAIN`, dynamic CORS validation for registered `hrms.tenant_profiles.subdomain` origins, reserved subdomain protection, and host-aware Setika login validation that rejects tokens whose tenant id does not match the tenant resolved from `Host`. Research outcome: multi-tenant SaaS HR portals generally route each tenant through a verified subdomain/custom domain, while browser security guidance requires exact allowed origins for credentialed cross-origin calls; Setika now prefers same-origin relative API calls and keeps CORS as a dynamic allow-list for registered tenant domains. Checks: `go test ./...` in `setika`. | Frontend API calls now default to same-origin relative paths so `tenant.dev.setika.one` and later `tenant.setika.one` do not require per-tenant frontend rebuilds or manual CORS entries. Dev compose passes `TENANT_BASE_DOMAIN=dev.setika.one` and forces an empty `NEXT_PUBLIC_API_URL` build arg so server `.env` cannot accidentally bake `https://dev.setika.one` into the frontend bundle. Deployed correction verified: running bundle no longer contains `https://dev.setika.one`; `aanvi.dev.setika.one` reaches login and returns credential-level responses, while unregistered `aanvi-infotech.dev.setika.one` reaches the server and returns `tenant domain not found`. Checks: `npm run build`. | login, tenant branding, CORS, tenant provisioning |
| HRMS-112 | P0 - Tenant Subdomain Safety Validation | Complete | Added tenant-domain creation/update safeguards in the tenant profile, tenant provisioning, and branding services so tenant subdomains must be stable short tenant codes, cannot use reserved platform names, cannot use confusing legal/business suffix aliases like `aanvi-infotech`, and cannot collide with another tenant's registered subdomain or display-name collision key. Auto-derived provisioning slugs now derive `aanvi` from `Aanvi Infotech` instead of creating `aanvi-infotech`. Research outcome: SaaS HR products with tenant portals treat the subdomain as a tenant identifier, not a marketing name; secure systems prevent ambiguous aliases and enforce host-to-tenant binding during authentication. | Added matching frontend validation in tenant setup/edit and public signup forms so users are stopped before submission. Existing host-aware login enforcement from HRMS-111 continues to reject users authenticating through another registered tenant subdomain, for example an Aanvi user logging in through `mashvirtual.dev.setika.one`. Checks: `go test ./core/domain`, `go test ./internal/adapters/httpx/handlers`, `npm run build`. | tenant provisioning, branding, signup, login security |
| HRMS-113 | P0 - Tenant Admin Portal Branding Menu | Complete | Exposed tenant-owned portal branding as a tenant-admin self-service setup item instead of only as a super-admin tenant-list row action. Added current-tenant `/hrms/branding` GET/PUT endpoints and guarded the existing `/hrms/tenants/{tenantID}/branding` route so non-super-admin users cannot target another tenant by changing the path tenant id. The current-tenant update endpoint always uses the authenticated tenant's registered subdomain, keeping tenant URL changes out of the normal branding form. Research outcome: modern SaaS admin consoles place tenant logo, favicon, colors, display name, and portal URL under Account/Company/Portal settings, while domain/subdomain changes are treated as higher-risk governed settings. Checks: `go test ./...` in `spur-hrms` and `setika`. | Added tenant left navigation `Setup -> Portal Branding` above `Branches`, a clean Portal Branding workspace with current portal URL, brand asset summary, and live preview, plus a modal-only editor for logo, favicon, display name, colors, layout, font, and preloader. Tenant admins see the portal URL as read-only; super-admin tenant-list branding remains available for support/override and now validates subdomains before save. Checks: `npm run build`. | `dashboard.html`, tenant setup navigation, branding modal |
| HRMS-113A | P0 - Runtime tenant branding consistency | Complete | No backend changes. Research outcome: Microsoft Entra and Okta branding guidance separates tenant sign-in identity elements such as header logo/favicon/colors from app behavior, and users expect tenant branding to appear consistently once the tenant context is known. Setika now treats authenticated tenant branding as the dashboard runtime theme, not only a public login-page lookup. | Login now shows one tenant logo at the upper-left of the sign-in area, removes the duplicate center/marketing logo and Setika-branded login copy, and leaves only a small `Powered by Setika` footer. Dashboard shell now loads the authenticated tenant's branding after `/auth/me`, uses the tenant logo in the left rail, keeps only a small `Powered by Setika` footer, scopes existing fixed-green module accents to tenant primary/secondary/tertiary colors, and applies portal-branding edits immediately while the modal is open; cancel restores the previous saved theme. Checks: `npm run build`; in-app browser validation was attempted but blocked by browser runtime sandbox metadata before navigation. | `login.html`, `dashboard.html`, portal branding runtime |
| HRMS-113B | P0 - Branding logo normalization and command launcher cleanup | Complete | No backend changes. Research outcome: tenant branding systems such as Microsoft Entra define explicit header/banner logo and favicon target dimensions, and identity portals keep launcher-style creation controls out of the primary chrome when the action is not a clear user workflow. Setika now normalizes uploaded tenant logos into a fixed header canvas and keeps creation flows inside their owning modules instead of a global `New` modal. | Tenant logo uploads now resize client-side into a centered transparent `245x48` PNG canvas, favicons into a centered square canvas, and the dashboard left-rail logo renders in a fixed prominent slot so tenant logos align consistently with the Setika logo treatment. Removed the global top-bar `New` button and the Command Center page `New` button/modal entry; Command Center keeps only `Refresh` and workflow-specific lane actions. Checks: `npm run build`. | `dashboard.html`, `HRCommandCenterSection.tsx`, portal branding upload |
| HRMS-113C | P0 - HR Command Center executive view cleanup | Complete | No backend changes. Research outcome: HR executive dashboards are expected to combine actionable work queues with workforce operating signals such as risk pressure, overdue work, approvals, employee requests, payroll blockers, movement, and AI/insight prompts; dense quick-action link lists are less useful than compact metrics and visual context for tenant admins. | Reworked the Command Center into `Today's HR Pulse` with a smaller, clearer header and explanatory context, compact colored KPI signal cards, tighter lane cards, and an `Operating picture` panel with ring metrics, proportional bars, and heaviest-lane summary. Removed the right-side Quick Actions block completely and deleted the unused global command launcher component file. Checks: `npm run build`. | `HRCommandCenterSection.tsx`, command center dashboard |
| HRMS-113D | P0 - Tenant Admin Business Dashboard Redesign | Complete | No backend changes; reused the existing `/dashboard/hr` analytics API instead of the operations workbench API. Research outcome: BambooHR, HiBob, Zoho People, SAP SuccessFactors, and comparable HR suites separate action queues from executive dashboards. Admin dashboards emphasize people mix, payroll readiness, hiring/onboarding movement, workforce reliability, reports/data health, configurable analytics, and visual trend signals. Work items belong in Workbench, not the landing dashboard. | Replaced the HR landing component with a compact business dashboard: smaller animated signal cards for People, Payroll, Hiring, Performance, and Reports; workforce mix chart, payroll readiness ring and pay composition bar, hiring/onboarding funnel, attendance/performance trend chart, reporting-health ring, and alert strip. Removed `Open Work`, `Due Today`, lane cards, and `Priority queue` from the landing dashboard. Renamed visible navigation from `Command Center` to `Business Dashboard` while keeping the old route alias for compatibility. Checks: `npm run build`. | `HRCommandCenterSection.tsx`, `dashboard.html`, tenant admin dashboard |
| HRMS-113E | P0 - Authenticated Dashboard Density and Copy Cleanup | Complete | No backend changes. Research/data-visualization outcome: dashboard first view should prioritize evidence-bearing metrics and direct labels; explanatory copy after login should be hidden behind contextual info only. | Added the no-marketing/static-copy-after-login rule to root and HRMS `AGENTS.md`. Removed the visible dashboard description and tenant-admin label strip, moved updated time and period controls into the compact dashboard header, reduced KPI card height, added color-tinted metric cards/panels, replaced the oversized People block with compact people facts and department bars, and tightened alert/panel spacing. Checks: `npm run build`. | `AGENTS.md`, `spur-hrms/AGENTS.md`, `HRCommandCenterSection.tsx`, tenant admin dashboard |
| HRMS-113F | P0 - Dashboard Alert Diagnostics with Actions | Complete | No backend changes; reused existing dashboard alert keys. Research/data-visualization outcome: monitoring dashboards should make alerts directly actionable by showing source, location, metric, and next step; severity-only warning cards create ambiguity and do not help administrators resolve issues. | Replaced plain warning/info alert cards with actionable diagnostics. Each alert now shows module area, exact `Where` path, `What` detail, count metric, severity, and a direct action button to Attendance, Leave Approvals, Payslips, Employees, or Reports depending on the alert key. Checks: `npm run build`. | `HRCommandCenterSection.tsx`, tenant admin dashboard alerts |
| HRMS-113G | P0 - Frontend Dashboard SmartHR Shell Redesign | Complete | No backend, schema, tenant/security, permission, or data migration changes. Research outcome: Zoho People, greytHR, and comparable HRMS products center admin dashboards around attendance, leave, payroll, onboarding/tasks, employee self-service, compliance, and analytics, while the supplied SmartHR template uses a fixed topbar/sidebar, compact search, KPI cards, bordered panels, and dense action surfaces. Setika used that structure without adding marketing copy after login. | Redesigned `/dashboard` shell and super-admin landing in `frontend/src/app/dashboard/page.tsx` with native Tailwind classes instead of shadcn-style UI primitives, using `tailwind/template/src/index.html` as the visual reference: fixed 50px topbar, 252px left rail, compact search/action controls, 5px panels, tighter KPI cards, SmartHR-style tenant table spacing, and responsive mobile sidebar. Checks: `npm run build`; browser QA via local Chrome DevTools fallback after in-app Browser failed on sandbox metadata, with desktop dashboard, Tenants interaction, and mobile sidebar screenshots reviewed for spacing, overlap, clipping, and control fit. | `tailwind/template/src/index.html`, `dashboard.html` |
| HRMS-113H | P0 - Local Setika Domain Deployment QA | Complete | Rebuilt and ran the local Docker stack with `TENANT_BASE_DOMAIN=setika.com`, same-origin frontend API calls, allowed local Setika origins, and an nginx port-80 proxy for `setika.com`, `www.setika.com`, `dev.setika.com`, `aanvi.setika.com`, and `mashvirtual.setika.com`. Existing `aanvi` and `mashvirtual` tenant profiles were confirmed in local Postgres; no tenant seed insert was needed. Checks: Docker health, `/healthz`, branding resolve, tenant-scoped login failure, unknown-subdomain rejection. | Fixed mobile login overflow discovered during domain QA by constraining the login form to the device viewport and allowing the remember/forgot row to wrap. Verified `aanvi.setika.com/login` and authenticated `dev.setika.com/dashboard` with Chrome DevTools device emulation on desktop and mobile: no document-level horizontal overflow, clean dashboard rendering, and expected reserved-domain branding fallback for `dev.setika.com`. Checks: `npm run build`, Docker frontend rebuild. | `login.html`, `dashboard.html`, local Docker domain proxy |
| HRMS-113I | P0 - Local Tenant Admin Login Header Limit | Complete | Local-only proxy/deployment fix; no schema, production, tenant data, or password changes. Tenant-admin auth was failing after successful `/setika/auth/login` because the larger tenant-admin JWT exceeded nginx's default client header limit on the follow-up `/auth/me` request, producing a proxy-level `400` before the backend could validate the session. | Increased the local nginx proxy request-header buffers (`client_header_buffer_size 16k`, `large_client_header_buffers 8 32k`) and reloaded `setika-local-proxy`. Retested `https://aanvi.setika.com/login -> /dashboard` with a disposable verified Aanvi tenant-admin account; login stayed on `/dashboard`, `/auth/me` returned `200`, the Business Dashboard rendered, no console/network errors were captured, and the QA account was removed afterward. | local Docker domain proxy, tenant admin login |
| HRMS-113J | P0 - Designation Attendance Requirement | Complete | Added designation-level `attendance_required` defaulting to true for existing designations, exposed it through employee attendance resolution, skipped exempt designations from absence/report denominators, and blocked check-in/check-out for exempt employees. Added a tenant-admin API guard for changing this setting; HR users cannot change it. Research outcome: Zoho People supports designation-scoped attendance-specific policies and a present-by-default pattern for people who should not punch daily, while greytHR uses assigned attendance schemes/exceptions; Setika models the requirement as a designation policy flag rather than a role or per-employee setting. Checks: `sqlc generate`, `go test ./...` in `spur-hrms`, `go test ./...` in `setika`, `npm run build`, Docker rebuild/deploy, API tenant-admin/HR guard QA, local Chrome desktop/mobile UI QA. | Added tenant-admin-only controls on Designations, required/not-required status pills, employee punch suppression when attendance is not applicable, and attendance-admin disabled action messaging. Browser QA screenshots: `/tmp/setika-designation-attendance-desktop.png`, `/tmp/setika-designation-attendance-mobile.png`; QA users removed and test designation restored to attendance required. | `designations.html`, `attendance-employee.html`, `attendance-report.html` |
| HRMS-110G | P0 - Self Celebration Workbench Suppression | Complete | Updated celebration notification fallback copy only; no schema changes. Research outcome: employee recognition and engagement products surface birthdays/work anniversaries as coworker recognition prompts, while the celebrated employee should see a greeting or neutral personal event, not an action to wish themselves. | Workbench now receives the logged-in user id and filters out that user's own personal celebration rows, so Dinesh Rana will not get a Workbench task to wish Dinesh Rana. Employee Dashboard still shows self celebrations as `Your Birthday`/personal event, and scheduled celebration fallback messages now say `Happy Birthday, Name` for the celebrated employee instead of `Birthday: Name`. Checks: `go test ./...`, `npm run build`. | workbench, employee dashboard, celebration jobs |
| HRMS-114 | P0 - Public Tenant Careers and Job Position Publishing | Complete | Added public careers APIs that resolve tenant branding by host, expose only published/open job postings, return tenant career-page content from `tenant-settings/careers`, and accept public applications by creating candidate plus candidate-application records with source `Career Portal`. Research outcome: Greenhouse-style job boards expose public unauthenticated GET endpoints for published jobs, separate full JD/application fields from compact listing cards, and submit applications through a controlled POST flow; Setika adopts the same public-read/protected-write split while validating tenant host and posting status. Applicant identity account/`APPLICANT` role creation is tracked as the next identity-layer integration because HRMS module handlers do not own identity user creation. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`. | Added `/careers` tenant public page with tenant logo/colors, company/about/core-values content, opening cards, JD modal, and application modal. Refactored `Hiring -> Job Positions` into a full-width grid with modal-only new/edit/location forms, career-page content modal, and an `Activate` action that creates and publishes a public job posting with JD sections, salary/experience, and expiry. Checks: `npm run build`. | `careers`, `JobPositionsSection.tsx`, public hiring APIs |
| HRMS-114A | P0 - Applicant Identity Account and Portal Role | Complete | Added tenant-scoped applicant identity creation/reuse in the Setika host layer through `/setika/applicants/apply`, introduced `APPLICANT` role template with only `applicant.portal.view`, added candidate-to-identity mapping table with consent/IP/metadata audit fields, and prevented startup baseline role sync from auto-assigning Employee to applicant-only users. Research outcome: Greenhouse-style job boards keep published jobs public while controlled application submission captures job-specific fields, consent, and application status; Setika follows that by keeping public job browsing unauthenticated while creating a narrow applicant identity for status tracking only. | Frontend careers apply modal now collects password confirmation and consent, submits through the host applicant endpoint, and applicant users land on a private `My Applications` portal instead of the employee dashboard/workbench. Checks: `sqlc generate`, `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache GOMODCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gomodcache go test ./...`, `GOCACHE=/Users/dinesh/workplace/setika-new/setika/.gocache go test ./...`, `npm run build`. | `careers`, identity signup, applicant portal |
| HRMS-114B | P0 - Tenant Public Home and Job SEO | Complete | Extended the public careers payload into a tenant public-site profile using existing tenant settings: welcome content, notices, SEO metadata, featured opening ids, published openings, and per-job public URLs/slugs. Base Setika domain remains the platform home, while tenant subdomains resolve to tenant-owned public pages. Research outcome: Google Search Central expects crawlable job URLs with `JobPosting` JSON-LD, canonical URLs, clear title/description metadata, valid expiry handling, direct-apply signals, and normal internal links/sitemaps so job pages can be discovered; Setika moved job details out of modal-only discovery and added server-rendered job pages. Checks: `GOCACHE=/Users/dinesh/workplace/setika-new/spur-hrms/.gocache go test ./...`. | Added tenant-aware `/` rendering for subdomains, server-rendered `/jobs/[slug]` SEO job detail pages with `JobPosting` JSON-LD, compatibility `/careers`, and a `Hiring -> Career Site` tenant admin editor for welcome message, notices, HR values, SEO title/description, CTA/login labels, and featured openings. Checks: `npm run build`. | public tenant home, `JobPositionsSection.tsx`, careers/job SEO |
