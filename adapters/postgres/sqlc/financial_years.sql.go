// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: financial_years.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const clearActiveFinancialYears = `-- name: ClearActiveFinancialYears :exec
UPDATE hrms.financial_years
SET is_active = FALSE,
    updated_by = $2,
    updated_at = NOW()
WHERE tenant_id = $1 AND NOT inactive
`

type ClearActiveFinancialYearsParams struct {
	TenantID  uuid.UUID   `db:"tenant_id" json:"tenant_id"`
	UpdatedBy pgtype.UUID `db:"updated_by" json:"updated_by"`
}

func (q *Queries) ClearActiveFinancialYears(ctx context.Context, arg ClearActiveFinancialYearsParams) error {
	_, err := q.db.Exec(ctx, clearActiveFinancialYears, arg.TenantID, arg.UpdatedBy)
	return err
}

const createFinancialYear = `-- name: CreateFinancialYear :one
INSERT INTO hrms.financial_years (
    tenant_id,
    name,
    start_date,
    end_date,
    is_active,
    payroll_year,
    leave_year,
    holiday_year,
    reporting_year,
    close_note,
    created_by,
    updated_by
) VALUES (
    $1, $2, $3, $4, FALSE, $5, $6, $7, $8, $9, $10, $10
)
RETURNING id, tenant_id, name, start_date, end_date, is_active, inactive, created_at, created_by, updated_at, updated_by, payroll_year, leave_year, holiday_year, reporting_year, is_locked, locked_at, locked_by, close_note
`

type CreateFinancialYearParams struct {
	TenantID      uuid.UUID   `db:"tenant_id" json:"tenant_id"`
	Name          string      `db:"name" json:"name"`
	StartDate     pgtype.Date `db:"start_date" json:"start_date"`
	EndDate       pgtype.Date `db:"end_date" json:"end_date"`
	PayrollYear   bool        `db:"payroll_year" json:"payroll_year"`
	LeaveYear     bool        `db:"leave_year" json:"leave_year"`
	HolidayYear   bool        `db:"holiday_year" json:"holiday_year"`
	ReportingYear bool        `db:"reporting_year" json:"reporting_year"`
	CloseNote     pgtype.Text `db:"close_note" json:"close_note"`
	CreatedBy     pgtype.UUID `db:"created_by" json:"created_by"`
}

func (q *Queries) CreateFinancialYear(ctx context.Context, arg CreateFinancialYearParams) (HrmsFinancialYear, error) {
	row := q.db.QueryRow(ctx, createFinancialYear,
		arg.TenantID,
		arg.Name,
		arg.StartDate,
		arg.EndDate,
		arg.PayrollYear,
		arg.LeaveYear,
		arg.HolidayYear,
		arg.ReportingYear,
		arg.CloseNote,
		arg.CreatedBy,
	)
	var i HrmsFinancialYear
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.Inactive,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.UpdatedAt,
		&i.UpdatedBy,
		&i.PayrollYear,
		&i.LeaveYear,
		&i.HolidayYear,
		&i.ReportingYear,
		&i.IsLocked,
		&i.LockedAt,
		&i.LockedBy,
		&i.CloseNote,
	)
	return i, err
}

const getActiveFinancialYear = `-- name: GetActiveFinancialYear :one
SELECT id, tenant_id, name, start_date, end_date, is_active, inactive, created_at, created_by, updated_at, updated_by, payroll_year, leave_year, holiday_year, reporting_year, is_locked, locked_at, locked_by, close_note FROM hrms.financial_years
WHERE tenant_id = $1 AND is_active AND NOT inactive
`

func (q *Queries) GetActiveFinancialYear(ctx context.Context, tenantID uuid.UUID) (HrmsFinancialYear, error) {
	row := q.db.QueryRow(ctx, getActiveFinancialYear, tenantID)
	var i HrmsFinancialYear
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.Inactive,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.UpdatedAt,
		&i.UpdatedBy,
		&i.PayrollYear,
		&i.LeaveYear,
		&i.HolidayYear,
		&i.ReportingYear,
		&i.IsLocked,
		&i.LockedAt,
		&i.LockedBy,
		&i.CloseNote,
	)
	return i, err
}

const getFinancialYear = `-- name: GetFinancialYear :one
SELECT id, tenant_id, name, start_date, end_date, is_active, inactive, created_at, created_by, updated_at, updated_by, payroll_year, leave_year, holiday_year, reporting_year, is_locked, locked_at, locked_by, close_note FROM hrms.financial_years
WHERE tenant_id = $1 AND id = $2 AND NOT inactive
`

type GetFinancialYearParams struct {
	TenantID uuid.UUID `db:"tenant_id" json:"tenant_id"`
	ID       uuid.UUID `db:"id" json:"id"`
}

func (q *Queries) GetFinancialYear(ctx context.Context, arg GetFinancialYearParams) (HrmsFinancialYear, error) {
	row := q.db.QueryRow(ctx, getFinancialYear, arg.TenantID, arg.ID)
	var i HrmsFinancialYear
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.Inactive,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.UpdatedAt,
		&i.UpdatedBy,
		&i.PayrollYear,
		&i.LeaveYear,
		&i.HolidayYear,
		&i.ReportingYear,
		&i.IsLocked,
		&i.LockedAt,
		&i.LockedBy,
		&i.CloseNote,
	)
	return i, err
}

const listFinancialYears = `-- name: ListFinancialYears :many
SELECT id, tenant_id, name, start_date, end_date, is_active, inactive, created_at, created_by, updated_at, updated_by, payroll_year, leave_year, holiday_year, reporting_year, is_locked, locked_at, locked_by, close_note FROM hrms.financial_years
WHERE tenant_id = $1 AND NOT inactive
ORDER BY start_date DESC, end_date DESC
`

func (q *Queries) ListFinancialYears(ctx context.Context, tenantID uuid.UUID) ([]HrmsFinancialYear, error) {
	rows, err := q.db.Query(ctx, listFinancialYears, tenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []HrmsFinancialYear
	for rows.Next() {
		var i HrmsFinancialYear
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Name,
			&i.StartDate,
			&i.EndDate,
			&i.IsActive,
			&i.Inactive,
			&i.CreatedAt,
			&i.CreatedBy,
			&i.UpdatedAt,
			&i.UpdatedBy,
			&i.PayrollYear,
			&i.LeaveYear,
			&i.HolidayYear,
			&i.ReportingYear,
			&i.IsLocked,
			&i.LockedAt,
			&i.LockedBy,
			&i.CloseNote,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markFinancialYearActive = `-- name: MarkFinancialYearActive :one
UPDATE hrms.financial_years
SET is_active = TRUE,
    updated_by = $3,
    updated_at = NOW()
WHERE tenant_id = $1 AND id = $2 AND NOT inactive AND NOT is_locked
RETURNING id, tenant_id, name, start_date, end_date, is_active, inactive, created_at, created_by, updated_at, updated_by, payroll_year, leave_year, holiday_year, reporting_year, is_locked, locked_at, locked_by, close_note
`

type MarkFinancialYearActiveParams struct {
	TenantID  uuid.UUID   `db:"tenant_id" json:"tenant_id"`
	ID        uuid.UUID   `db:"id" json:"id"`
	UpdatedBy pgtype.UUID `db:"updated_by" json:"updated_by"`
}

func (q *Queries) MarkFinancialYearActive(ctx context.Context, arg MarkFinancialYearActiveParams) (HrmsFinancialYear, error) {
	row := q.db.QueryRow(ctx, markFinancialYearActive, arg.TenantID, arg.ID, arg.UpdatedBy)
	var i HrmsFinancialYear
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.Inactive,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.UpdatedAt,
		&i.UpdatedBy,
		&i.PayrollYear,
		&i.LeaveYear,
		&i.HolidayYear,
		&i.ReportingYear,
		&i.IsLocked,
		&i.LockedAt,
		&i.LockedBy,
		&i.CloseNote,
	)
	return i, err
}

const setFinancialYearLock = `-- name: SetFinancialYearLock :one
UPDATE hrms.financial_years
SET is_locked = $3,
    locked_at = CASE WHEN $3 THEN NOW() ELSE NULL END,
    locked_by = CASE WHEN $3 THEN $4 ELSE NULL END,
    close_note = $5,
    updated_by = $4,
    updated_at = NOW()
WHERE tenant_id = $1 AND id = $2 AND NOT inactive
RETURNING id, tenant_id, name, start_date, end_date, is_active, inactive, created_at, created_by, updated_at, updated_by, payroll_year, leave_year, holiday_year, reporting_year, is_locked, locked_at, locked_by, close_note
`

type SetFinancialYearLockParams struct {
	TenantID  uuid.UUID   `db:"tenant_id" json:"tenant_id"`
	ID        uuid.UUID   `db:"id" json:"id"`
	IsLocked  bool        `db:"is_locked" json:"is_locked"`
	UpdatedBy pgtype.UUID `db:"updated_by" json:"updated_by"`
	CloseNote pgtype.Text `db:"close_note" json:"close_note"`
}

func (q *Queries) SetFinancialYearLock(ctx context.Context, arg SetFinancialYearLockParams) (HrmsFinancialYear, error) {
	row := q.db.QueryRow(ctx, setFinancialYearLock,
		arg.TenantID,
		arg.ID,
		arg.IsLocked,
		arg.UpdatedBy,
		arg.CloseNote,
	)
	var i HrmsFinancialYear
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.Inactive,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.UpdatedAt,
		&i.UpdatedBy,
		&i.PayrollYear,
		&i.LeaveYear,
		&i.HolidayYear,
		&i.ReportingYear,
		&i.IsLocked,
		&i.LockedAt,
		&i.LockedBy,
		&i.CloseNote,
	)
	return i, err
}

const softDeleteFinancialYear = `-- name: SoftDeleteFinancialYear :exec
UPDATE hrms.financial_years
SET inactive = TRUE,
    is_active = FALSE,
    updated_by = $3,
    updated_at = NOW()
WHERE tenant_id = $1 AND id = $2 AND NOT inactive AND NOT is_locked
`

type SoftDeleteFinancialYearParams struct {
	TenantID  uuid.UUID   `db:"tenant_id" json:"tenant_id"`
	ID        uuid.UUID   `db:"id" json:"id"`
	UpdatedBy pgtype.UUID `db:"updated_by" json:"updated_by"`
}

func (q *Queries) SoftDeleteFinancialYear(ctx context.Context, arg SoftDeleteFinancialYearParams) error {
	_, err := q.db.Exec(ctx, softDeleteFinancialYear, arg.TenantID, arg.ID, arg.UpdatedBy)
	return err
}

const updateFinancialYear = `-- name: UpdateFinancialYear :one
UPDATE hrms.financial_years
SET name = $3,
    start_date = $4,
    end_date = $5,
    payroll_year = $6,
    leave_year = $7,
    holiday_year = $8,
    reporting_year = $9,
    close_note = $10,
    updated_by = $11,
    updated_at = NOW()
WHERE tenant_id = $1 AND id = $2 AND NOT inactive AND NOT is_locked
RETURNING id, tenant_id, name, start_date, end_date, is_active, inactive, created_at, created_by, updated_at, updated_by, payroll_year, leave_year, holiday_year, reporting_year, is_locked, locked_at, locked_by, close_note
`

type UpdateFinancialYearParams struct {
	TenantID      uuid.UUID   `db:"tenant_id" json:"tenant_id"`
	ID            uuid.UUID   `db:"id" json:"id"`
	Name          string      `db:"name" json:"name"`
	StartDate     pgtype.Date `db:"start_date" json:"start_date"`
	EndDate       pgtype.Date `db:"end_date" json:"end_date"`
	PayrollYear   bool        `db:"payroll_year" json:"payroll_year"`
	LeaveYear     bool        `db:"leave_year" json:"leave_year"`
	HolidayYear   bool        `db:"holiday_year" json:"holiday_year"`
	ReportingYear bool        `db:"reporting_year" json:"reporting_year"`
	CloseNote     pgtype.Text `db:"close_note" json:"close_note"`
	UpdatedBy     pgtype.UUID `db:"updated_by" json:"updated_by"`
}

func (q *Queries) UpdateFinancialYear(ctx context.Context, arg UpdateFinancialYearParams) (HrmsFinancialYear, error) {
	row := q.db.QueryRow(ctx, updateFinancialYear,
		arg.TenantID,
		arg.ID,
		arg.Name,
		arg.StartDate,
		arg.EndDate,
		arg.PayrollYear,
		arg.LeaveYear,
		arg.HolidayYear,
		arg.ReportingYear,
		arg.CloseNote,
		arg.UpdatedBy,
	)
	var i HrmsFinancialYear
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.Inactive,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.UpdatedAt,
		&i.UpdatedBy,
		&i.PayrollYear,
		&i.LeaveYear,
		&i.HolidayYear,
		&i.ReportingYear,
		&i.IsLocked,
		&i.LockedAt,
		&i.LockedBy,
		&i.CloseNote,
	)
	return i, err
}
